# Phase 4 ORM 핵심 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: `superpowers:subagent-driven-development` (recommended) 또는 `superpowers:executing-plans`로 task-by-task 실행. Step은 체크박스(`- [ ]`) 형식.

**Goal:** sfs-tx 위에 *영속성 컨텍스트(1차 캐시)* + *snapshot 기반 더티 체킹* + *byte-buddy lazy proxy*를 얹어, *영속성 컨텍스트가 entity identity의 단일 source of truth*가 되는 ORM 메커니즘을 own minimal API로 박제. Phase 1A gap *책임 분담 패턴*의 *대칭 회수* (단일 SoT 패턴)가 학습 정점.

**Architecture:** sfs-orm 모듈 신설 (sfs-tx 의존, sfs-aop 비의존, sfs-context 비의존 — layering 정합). byte-buddy 직접 사용 (sfs-aop와 도구 공유, 도메인 분리). Hibernate 본가 패턴(C1: 1 entity = 1 instance) 채택 — proxy도 캐시 entry 그 자체. EntityManager는 트랜잭션 바운드(L1) — TSM resource로 등록되어 트랜잭션 종료 시 자동 flush + close.

**Tech Stack:** Java 25 LTS · JUnit 5 + AssertJ · byte-buddy 1.14 (Phase 2A 도입분 재사용) · H2 (sfs-tx 도입분 재사용)

**선행 의존:** Phase 1 (IoC), Phase 2 (AOP), Phase 3 (트랜잭션), Phase 1A 결함 보강 — 모두 완료 + main 머지 (회귀 244 PASS / 0 FAIL)

**연관 spec:** `docs/superpowers/specs/2026-05-04-phase-4-orm-design.md`

**브랜치:** `feat/phase4-jpa` (main `4126d0b`에서 분기)

---

## 0. 실행 가이드

각 Task는 다음 사이클을 따른다 (CLAUDE.md "플랜 실행 루틴"):

1. 실패 테스트 작성 *(TDD 적용 대상에 한함, spec § 7.1 분류)*
2. `./gradlew :<module>:test --tests <TestName>` 로 FAIL 확인
3. 최소 구현
4. PASS 확인 *(TDD 제외 대상은 1~2 생략, 3 이후 컴파일 + 회귀 테스트만)*
5. 한국어 커밋 메시지로 커밋
6. Plan 문서의 해당 Step 체크박스 `- [ ]` → `- [x]` 업데이트
7. 실행 중 편차 발생 시 `> **실행 기록 (YYYY-MM-DD):**` 블록으로 박제

### 0.1 TDD 적용/제외 (spec § 7.1 정합)

**TDD 적용 (필수)**:
- `PersistenceContext`, `EntityMetadataAnalyzer`, `EntityPersister`, `LazyProxyFactory`, `LazyInterceptor`, `IdentifierGenerator` 2 구현체 — 분기/상태/알고리즘
- `SfsEntityManager` 구현 (persist/find/remove/flush/merge) — 분기 + 라이프사이클
- `SfsTransactionalEntityManager` proxy + `SfsEntityManagerFactoryBean` — TSM 통합
- 모든 예외 발생 시나리오 — 통합 테스트가 박제

**TDD 제외 (회귀 검증만)**:
- 어노테이션 6개 (단순 마커, 통합 테스트가 회귀망)
- `EntityMetadata`/`FieldMetadata`/`RelationMetadata` records (DTO)
- `SfsEntityManager` 인터페이스 (시그니처만)
- `SfsEntityManagerFactory` fluent builder (통합 테스트로 간접 검증)
- 예외 클래스 정의 (단순 RuntimeException 상속)
- sfs-orm 모듈 신설 (build.gradle.kts, settings.gradle.kts)
- sfs-samples 도메인 record/POJO + schema.sql + OrmDemoApplication main

### 0.2 커밋 컨벤션 (CLAUDE.md 정합)

- 한국어 커밋 메시지
- `test(<module>): ...` (테스트 추가)
- `feat(<module>): ...` (동작 변경)
- `refactor(<module>): ...` (단순화)
- `docs: ...` (plan 체크박스 / spec 갱신)

### 0.3 회귀 카운트

| 시작 | 목표 | 차이 |
|---|---|---|
| 244 PASS (Phase 1A gap 마감) | **296 PASS** | **+52** (sfs-tx +2 PRE, sfs-orm 단위 33 + 통합 19 — § 7.4) |

### 0.4 모듈 의존 그래프 (spec § 2.1)

```
sfs-samples ──► sfs-orm ──► sfs-tx ──► sfs-aop ──► sfs-beans ──► sfs-core
                  │
                  └─► byte-buddy (직접, sfs-aop 비의존)
```

---

## 1. 섹션 구조 (Task 한 줄 요약)

| Task | 내용 | 모듈 | TDD | DoD 매핑 | 회귀 |
|---|---|---|---|---|---|
| **PRE** | `JdbcTemplate`에 `queryForObject` + `updateAndReturnKey` 메서드 2종 추가 | sfs-tx | 적용 | 1.5 | +2 |
| **A1** | sfs-orm 모듈 신설 + `settings.gradle.kts` + `build.gradle.kts` | sfs-orm | 제외 | 1 | +0 |
| **A2** | 어노테이션 6개 (`@SfsEntity`, `@SfsId`, `@SfsGeneratedValue`, `@SfsColumn`, `@SfsManyToOne`, `@SfsJoinColumn`) | sfs-orm | 제외 | 2 | +0 |
| **A3** | 예외 4개 (`SfsPersistenceException` 계층) | sfs-orm | 제외 | 3 | +0 |
| **B1** | `EntityMetadata`/`FieldMetadata`/`RelationMetadata` records | sfs-orm | 제외 | 5(간접) | +0 |
| **B2** | `EntityMetadataAnalyzer` — 어노테이션 분석 + fail-fast 5종 검증 | sfs-orm | 적용 | 4 | +8 |
| **C1** | `SfsEntityManager` 인터페이스 + `SfsEntityManagerFactory` (fluent builder) | sfs-orm | 제외 + 통합 | 10 | +0 |
| **D1** | `PersistenceContext` — identityMap, snapshots, actionQueue, EntityKey, EntityAction sealed | sfs-orm | 적용 | 5 | +6 |
| **E1** | `IdentifierGenerator` 인터페이스 + `IdentityGenerator` (post-insert) | sfs-orm | 적용 | 8 | +2 |
| **E2** | `SequenceGenerator` (pre-insert) | sfs-orm | 적용 | 8 | +2 |
| **F1** | `EntityPersister` — INSERT/SELECT/DELETE SQL 생성 + 실행 | sfs-orm | 적용 | 6 | +3 |
| **F2** | `EntityPersister` — UPDATE 동적 SQL (dirty 컬럼 BitSet) | sfs-orm | 적용 | 6 | +2 |
| **G1** | `RealEntityManager.persist()` — SEQUENCE 분기 (write-behind) | sfs-orm | 적용 | 7 | +1 (단위) |
| **G2** | `RealEntityManager.persist()` — IDENTITY 분기 (즉시 INSERT) | sfs-orm | 적용 | 7 | +1 (단위) |
| **H1** | `RealEntityManager.find()` — cache hit + miss SELECT + 컬럼 매핑 | sfs-orm | 적용 | 7 | +0 (G1과 같은 파일) |
| **I1** | `LazyProxyFactory` — byte-buddy 서브클래싱 + caching + getId 비-인터셉트 | sfs-orm | 적용 | 7 | +3 |
| **I2** | `LazyInterceptor` — target 채우기 + closed 예외 + identity 보장 | sfs-orm | 적용 | 7 | +3 |
| **J1** | `find()` 통합 — LAZY 필드 proxy 채우기 + EAGER 별도 SELECT (재귀) | sfs-orm | 적용 | 7 | +0 (H1 갱신) |
| **K1** | `remove()` — actionQueue.add(DeleteAction) | sfs-orm | 적용 | 10 | +0 (단위 흡수) |
| **K2** | `flush()` — Phase 1 dirty check + Phase 2 action 순차 실행 | sfs-orm | 적용 | 5 | +0 (단위 흡수) |
| **K3** | `merge()` — shallow copy + identity map 갱신 + return value 함정 | sfs-orm | 적용 | 10 | +0 (단위 흡수) |
| **L1** | `SfsEntityManagerFactoryBean` — bean lifecycle + TSM 콜백 등록 | sfs-orm | 적용 | 9 | +0 (통합에 흡수) |
| **L2** | `SfsTransactionalEntityManager` proxy — TSM lookup + `TransactionRequiredException` | sfs-orm | 적용 | 9 | +0 (통합에 흡수) |
| **M1** | 통합 — `BasicCrudIntegrationTest` (5건) | sfs-orm | 적용 | 12 | +5 |
| **M2** | 통합 — `DirtyCheckingIntegrationTest` (3건) + `IdentityMapIntegrationTest` (3건) | sfs-orm | 적용 | 12 | +6 |
| **M3** | 통합 — `LazyLoadingIntegrationTest` (4건) + `LazyInitializationExceptionTest` (2건) | sfs-orm | 적용 | 12 | +6 |
| **M4** | 통합 — `MergeIntegrationTest` (3건) + `TransactionRequiredExceptionTest` (1건) | sfs-orm | 적용 | 12 | +4 |
| **M5** | 통합 — `SequenceVsIdentityComparisonTest` (2건) | sfs-orm | 적용 | 12 | +2 |
| **N1** | sfs-samples 도메인 (`User`/`Order`/`AuditLog`) + `orm-schema.sql` + `OrmConfig` | sfs-samples | 제외 | 11 | +0 |
| **N2** | `OrmDemoApplication` main + service 메서드 (시연 7건 console) | sfs-samples | 제외 | 11 | +0 |
| **O1** | 전체 빌드 + 회귀 카운트 검증 + DoD 체크박스 갱신 | (전체) | 제외 | 12 | +0 |
| **O2** | 마감 게이트 3단계 (다관점 리뷰 + 리팩토링 + simplify) | (전체) | 제외 | 13 | +0 (별도) |

**총 31 task / +52 PASS / DoD 14 항목 모두 매핑**

---

## 2. File Structure (생성/수정 파일 매핑)

### 2.1 신설 파일

```
sfs-tx/src/main/java/com/choisk/sfs/tx/jdbc/
└── JdbcTemplate.java                         # PRE — queryForObject + updateAndReturnKey 추가

sfs-tx/src/test/java/com/choisk/sfs/tx/jdbc/
└── JdbcTemplateTest.java                     # PRE — 신규 메서드 2건 + 기존 회귀

sfs-orm/                                       # A1 신설
├── build.gradle.kts
└── src/main/java/com/choisk/sfs/orm/
    ├── annotation/
    │   ├── SfsEntity.java                    # A2
    │   ├── SfsId.java                        # A2
    │   ├── SfsGeneratedValue.java            # A2 (+ GenerationType enum)
    │   ├── SfsColumn.java                    # A2
    │   ├── SfsManyToOne.java                 # A2 (+ FetchType enum)
    │   └── SfsJoinColumn.java                # A2
    │
    ├── exception/
    │   ├── SfsPersistenceException.java       # A3
    │   ├── SfsLazyInitializationException.java # A3
    │   ├── SfsTransactionRequiredException.java # A3
    │   └── SfsEntityMappingException.java    # A3
    │
    ├── SfsEntityManager.java                 # C1 (인터페이스)
    ├── SfsEntityManagerFactory.java          # C1 (fluent builder + impl)
    │
    ├── support/
    │   ├── EntityMetadata.java               # B1 (record)
    │   ├── FieldMetadata.java                # B1 (record)
    │   ├── RelationMetadata.java             # B1 (record)
    │   ├── EntityMetadataAnalyzer.java       # B2
    │   ├── EntityKey.java                    # D1 (record)
    │   ├── EntityAction.java                 # D1 (sealed interface + records)
    │   ├── PersistenceContext.java           # D1
    │   ├── IdentifierGenerator.java          # E1 (인터페이스)
    │   ├── IdentityGenerator.java            # E1
    │   ├── SequenceGenerator.java            # E2
    │   ├── EntityPersister.java              # F1 + F2
    │   ├── RealEntityManager.java            # G1 (persist/find/remove/flush/merge 본체)
    │   ├── LazyProxyFactory.java             # I1
    │   └── LazyInterceptor.java              # I2
    │
    └── boot/
        ├── SfsEntityManagerFactoryBean.java  # L1
        └── SfsTransactionalEntityManager.java # L2

sfs-orm/src/test/java/com/choisk/sfs/orm/
├── support/                                   # 단위 테스트 33건
│   ├── PersistenceContextTest.java            # D1 (+6)
│   ├── EntityMetadataAnalyzerTest.java        # B2 (+8)
│   ├── EntityPersisterTest.java               # F1+F2 (+5)
│   ├── LazyProxyFactoryTest.java              # I1 (+3)
│   ├── LazyInterceptorTest.java               # I2 (+3)
│   ├── IdentityGeneratorTest.java             # E1 (+2)
│   ├── SequenceGeneratorTest.java             # E2 (+2)
│   ├── RealEntityManagerPersistTest.java      # G1+G2 (+2 단위)
│   └── (find/remove/flush/merge 단위는 통합 테스트로 흡수)
│
└── integration/                               # 통합 테스트 19건 (H2)
    ├── BasicCrudIntegrationTest.java          # M1 (+5)
    ├── DirtyCheckingIntegrationTest.java      # M2 (+3)
    ├── IdentityMapIntegrationTest.java        # M2 (+3)
    ├── LazyLoadingIntegrationTest.java        # M3 (+4)
    ├── LazyInitializationExceptionTest.java   # M3 (+2)
    ├── MergeIntegrationTest.java              # M4 (+3)
    ├── TransactionRequiredExceptionTest.java  # M4 (+1)
    └── SequenceVsIdentityComparisonTest.java  # M5 (+2)

sfs-orm/src/test/resources/
└── orm-schema-test.sql                        # 통합 테스트 공통 schema

sfs-samples/src/main/java/com/choisk/sfs/samples/orm/
├── OrmDemoApplication.java                    # N2
├── config/OrmConfig.java                      # N1
├── domain/User.java                           # N1
├── domain/Order.java                          # N1
├── domain/AuditLog.java                       # N1
└── service/OrderService.java                  # N2
└── service/UserService.java                   # N2

sfs-samples/src/main/resources/
└── orm-schema.sql                             # N1 (Phase 3 schema 확장)
```

### 2.2 수정 파일

```
settings.gradle.kts                            # A1 — sfs-orm 모듈 등록
sfs-tx/src/main/java/com/choisk/sfs/tx/jdbc/JdbcTemplate.java   # PRE — 메서드 2종 추가
gradle/libs.versions.toml (선택)               # 의존성 버전 catalog (이미 있는 것 활용)
docs/superpowers/plans/2026-05-04-phase-4-orm.md  # 본 plan — 체크박스 진행 갱신
```

### 2.3 갱신 (Phase 마감 시)

```
.../memory/MEMORY.md                           # Phase 4 마감 메모리 추가
.../memory/project_phase4_resume_point.md      # 신규 메모리 파일
```

---

## 섹션 PRE: sfs-tx JdbcTemplate 신규 메서드

본 phase가 sfs-orm을 신설하지만, *IdentifierGenerator가 호출할 메서드 2종*이 sfs-tx `JdbcTemplate`에 없음 — 먼저 추가.

### Task PRE: `JdbcTemplate`에 `queryForObject` + `updateAndReturnKey` 추가

**Files:**
- Modify: `sfs-tx/src/main/java/com/choisk/sfs/tx/jdbc/JdbcTemplate.java`
- Test: `sfs-tx/src/test/java/com/choisk/sfs/tx/jdbc/JdbcTemplateTest.java`

- [ ] **Step 1: 실패 테스트 작성 — `queryForObject` 단일 값 조회 + `updateAndReturnKey` generated key 반환 검증 (2건)**

`sfs-tx/src/test/java/com/choisk/sfs/tx/jdbc/JdbcTemplateTest.java` (기존 파일에 추가):

```java
@Test
void queryForObject_returns_single_scalar_value() {
    jdbcTemplate.update("CREATE SEQUENCE test_seq START WITH 100");
    Long next = jdbcTemplate.queryForObject(
        "SELECT NEXTVAL('test_seq')", Long.class);
    assertThat(next).isEqualTo(100L);
}

@Test
void updateAndReturnKey_returns_generated_identity() {
    jdbcTemplate.update("CREATE TABLE pre_test (id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, name VARCHAR(50))");
    Number key = jdbcTemplate.updateAndReturnKey(
        "INSERT INTO pre_test (name) VALUES (?)", "alice");
    assertThat(key).isNotNull();
    assertThat(key.longValue()).isEqualTo(1L);
}
```

- [ ] **Step 2: FAIL 확인**

```
./gradlew :sfs-tx:test --tests JdbcTemplateTest.queryForObject_returns_single_scalar_value
./gradlew :sfs-tx:test --tests JdbcTemplateTest.updateAndReturnKey_returns_generated_identity
```
Expected: 컴파일 에러 (메서드 미존재)

- [ ] **Step 3: 최소 구현 — `JdbcTemplate.java`에 두 메서드 추가**

```java
public <T> T queryForObject(String sql, Class<T> requiredType, Object... args) {
    List<T> results = query(sql, (rs, rowNum) -> rs.getObject(1, requiredType), args);
    if (results.isEmpty()) return null;
    if (results.size() > 1)
        throw new IllegalStateException("Expected single result, got " + results.size());
    return results.get(0);
}

public Number updateAndReturnKey(String sql, Object... args) {
    Connection conn = DataSourceUtils.getConnection(dataSource, tsm);
    try (PreparedStatement ps = conn.prepareStatement(sql, Statement.RETURN_GENERATED_KEYS)) {
        for (int i = 0; i < args.length; i++) {
            ps.setObject(i + 1, args[i]);
        }
        ps.executeUpdate();
        try (ResultSet keys = ps.getGeneratedKeys()) {
            if (keys.next()) return (Number) keys.getObject(1);
            throw new IllegalStateException("No generated key returned");
        }
    } catch (SQLException e) {
        throw new DataAccessException("updateAndReturnKey failed: " + sql, e);
    } finally {
        DataSourceUtils.releaseConnection(conn, dataSource, tsm);
    }
}
```

> *기존 JdbcTemplate에 `DataSourceUtils.getConnection`/`releaseConnection` 패턴이 이미 있다면 그 헬퍼 재사용. 현재 구조가 다르면 기존 `update()` 메서드의 Connection 처리 패턴을 그대로 따라할 것.*

- [ ] **Step 4: PASS 확인 + 회귀 검증**

```
./gradlew :sfs-tx:test
```
Expected: 244 + 2 = 246 PASS / 0 FAIL

- [ ] **Step 5: 커밋**

```bash
git add sfs-tx/src/main/java/com/choisk/sfs/tx/jdbc/JdbcTemplate.java \
        sfs-tx/src/test/java/com/choisk/sfs/tx/jdbc/JdbcTemplateTest.java
git commit -m "feat(sfs-tx): JdbcTemplate에 queryForObject + updateAndReturnKey 추가 — Phase 4 IdentifierGenerator 토대"
```

---

## 섹션 A: sfs-orm 모듈 + 어노테이션 + 예외

### Task A1: sfs-orm 모듈 신설

**Files:**
- Modify: `settings.gradle.kts`
- Create: `sfs-orm/build.gradle.kts`

- [ ] **Step 1: `settings.gradle.kts`에 sfs-orm 추가**

```kotlin
rootProject.name = "spring-from-scratch"

include(
    "sfs-core",
    "sfs-beans",
    "sfs-context",
    "sfs-aop",
    "sfs-tx",
    "sfs-orm",       // ← 추가
    "sfs-samples",
)
```

- [ ] **Step 2: `sfs-orm/build.gradle.kts` 작성**

```kotlin
plugins {
    `java-library`
}

dependencies {
    implementation(project(":sfs-tx"))
    implementation(libs.byteBuddy)
    
    testImplementation(libs.h2)
    testImplementation(project(":sfs-context"))   // 통합 테스트 한정
}
```

- [ ] **Step 3: 컴파일 확인**

```
./gradlew :sfs-orm:compileJava
```
Expected: BUILD SUCCESSFUL (소스 없으니 trivially)

- [ ] **Step 4: 커밋**

```bash
git add settings.gradle.kts sfs-orm/build.gradle.kts
git commit -m "feat(sfs-orm): 모듈 신설 + Gradle 의존 (sfs-tx + byte-buddy)"
```

---

### Task A2: 어노테이션 6개

**Files:** Create:
- `sfs-orm/src/main/java/com/choisk/sfs/orm/annotation/SfsEntity.java`
- `sfs-orm/src/main/java/com/choisk/sfs/orm/annotation/SfsId.java`
- `sfs-orm/src/main/java/com/choisk/sfs/orm/annotation/SfsGeneratedValue.java`
- `sfs-orm/src/main/java/com/choisk/sfs/orm/annotation/SfsColumn.java`
- `sfs-orm/src/main/java/com/choisk/sfs/orm/annotation/SfsManyToOne.java`
- `sfs-orm/src/main/java/com/choisk/sfs/orm/annotation/SfsJoinColumn.java`

> **TDD 제외** (단순 마커 어노테이션, 통합 테스트가 회귀망)

- [ ] **Step 1: 6개 어노테이션 작성 (한 커밋)**

`SfsEntity.java`:
```java
package com.choisk.sfs.orm.annotation;

import java.lang.annotation.*;

@Retention(RetentionPolicy.RUNTIME)
@Target(ElementType.TYPE)
public @interface SfsEntity {
    String name() default "";   // 빈 문자열이면 simple class name
}
```

`SfsId.java`:
```java
package com.choisk.sfs.orm.annotation;

import java.lang.annotation.*;

@Retention(RetentionPolicy.RUNTIME)
@Target(ElementType.FIELD)
public @interface SfsId { }
```

`SfsGeneratedValue.java`:
```java
package com.choisk.sfs.orm.annotation;

import java.lang.annotation.*;

@Retention(RetentionPolicy.RUNTIME)
@Target(ElementType.FIELD)
public @interface SfsGeneratedValue {
    GenerationType strategy();
    String sequenceName() default "";   // SEQUENCE 전략에서 필수
    
    enum GenerationType { IDENTITY, SEQUENCE }
}
```

`SfsColumn.java`:
```java
package com.choisk.sfs.orm.annotation;

import java.lang.annotation.*;

@Retention(RetentionPolicy.RUNTIME)
@Target(ElementType.FIELD)
public @interface SfsColumn {
    String name() default "";   // 빈 문자열이면 필드명 그대로
}
```

`SfsManyToOne.java`:
```java
package com.choisk.sfs.orm.annotation;

import java.lang.annotation.*;

@Retention(RetentionPolicy.RUNTIME)
@Target(ElementType.FIELD)
public @interface SfsManyToOne {
    FetchType fetch() default FetchType.LAZY;   // F1.5 default LAZY
    
    enum FetchType { LAZY, EAGER }
}
```

`SfsJoinColumn.java`:
```java
package com.choisk.sfs.orm.annotation;

import java.lang.annotation.*;

@Retention(RetentionPolicy.RUNTIME)
@Target(ElementType.FIELD)
public @interface SfsJoinColumn {
    String name();   // FK 컬럼명 (필수)
}
```

- [ ] **Step 2: 컴파일 확인**

```
./gradlew :sfs-orm:compileJava
```
Expected: BUILD SUCCESSFUL

- [ ] **Step 3: 커밋**

```bash
git add sfs-orm/src/main/java/com/choisk/sfs/orm/annotation/
git commit -m "feat(sfs-orm): own minimal API 어노테이션 6개 정의 — @SfsEntity/@SfsId/@SfsGeneratedValue/@SfsColumn/@SfsManyToOne/@SfsJoinColumn"
```

---

### Task A3: 예외 4개

**Files:** Create:
- `sfs-orm/src/main/java/com/choisk/sfs/orm/exception/SfsPersistenceException.java`
- `sfs-orm/src/main/java/com/choisk/sfs/orm/exception/SfsLazyInitializationException.java`
- `sfs-orm/src/main/java/com/choisk/sfs/orm/exception/SfsTransactionRequiredException.java`
- `sfs-orm/src/main/java/com/choisk/sfs/orm/exception/SfsEntityMappingException.java`

> **TDD 제외** (단순 RuntimeException 상속, 발생 시나리오는 통합 테스트가 박제)

- [ ] **Step 1: 4개 예외 작성 (한 커밋)**

```java
// SfsPersistenceException.java (root)
package com.choisk.sfs.orm.exception;

public class SfsPersistenceException extends RuntimeException {
    public SfsPersistenceException(String message) { super(message); }
    public SfsPersistenceException(String message, Throwable cause) { super(message, cause); }
}

// SfsLazyInitializationException.java
public class SfsLazyInitializationException extends SfsPersistenceException {
    public SfsLazyInitializationException(String entityKey) {
        super("Cannot initialize lazy proxy after persistence context closed: " + entityKey);
    }
}

// SfsTransactionRequiredException.java
public class SfsTransactionRequiredException extends SfsPersistenceException {
    public SfsTransactionRequiredException() {
        super("No transaction in progress — annotate the calling method with @Transactional");
    }
}

// SfsEntityMappingException.java
public class SfsEntityMappingException extends SfsPersistenceException {
    public SfsEntityMappingException(String message) { super(message); }
}
```

- [ ] **Step 2: 컴파일 확인 + 커밋**

```
./gradlew :sfs-orm:compileJava
```

```bash
git add sfs-orm/src/main/java/com/choisk/sfs/orm/exception/
git commit -m "feat(sfs-orm): 예외 4개 (SfsPersistenceException 계층) — Lazy/Tx/Mapping 박제 토대"
```

---

## 섹션 B: EntityMetadata + EntityMetadataAnalyzer

### Task B1: EntityMetadata records

**Files:** Create:
- `sfs-orm/src/main/java/com/choisk/sfs/orm/support/EntityMetadata.java`
- `sfs-orm/src/main/java/com/choisk/sfs/orm/support/FieldMetadata.java`
- `sfs-orm/src/main/java/com/choisk/sfs/orm/support/RelationMetadata.java`

> **TDD 제외** (record DTO, EntityMetadataAnalyzerTest가 회귀망)

- [ ] **Step 1: 3개 record 작성 (한 커밋)**

```java
// FieldMetadata.java
package com.choisk.sfs.orm.support;

import java.lang.reflect.Field;

public record FieldMetadata(Field field, String columnName, Class<?> javaType) { }

// RelationMetadata.java
package com.choisk.sfs.orm.support;

import com.choisk.sfs.orm.annotation.SfsManyToOne.FetchType;
import java.lang.reflect.Field;

public record RelationMetadata(
    Field field,
    FetchType fetch,
    Class<?> targetEntity,
    String joinColumnName
) { }

// EntityMetadata.java
package com.choisk.sfs.orm.support;

import java.util.List;

public record EntityMetadata(
    Class<?> entityClass,
    String tableName,
    FieldMetadata idField,
    IdentifierGenerator idGenerator,
    List<FieldMetadata> columns,
    List<RelationMetadata> manyToOnes,
    String insertSql,
    String selectByIdSql,
    String deleteSql
) {
    public boolean hasLazyFields() {
        return manyToOnes.stream().anyMatch(r -> r.fetch() == com.choisk.sfs.orm.annotation.SfsManyToOne.FetchType.LAZY);
    }
}
```

> *`IdentifierGenerator` 인터페이스는 Task E1에서 신설 — 본 step에서는 forward reference. import 가능한 forward 클래스 stub만 두고 E1에서 채움. 또는 record를 E1 후에 작성하도록 순서 조정 가능.*

- [ ] **Step 2: 임시 stub `IdentifierGenerator` 인터페이스 (E1에서 본격 구현)**

`IdentifierGenerator.java` (stub, E1에서 확장):
```java
package com.choisk.sfs.orm.support;

public interface IdentifierGenerator { }
```

- [ ] **Step 3: 컴파일 + 커밋**

```bash
./gradlew :sfs-orm:compileJava
git add sfs-orm/src/main/java/com/choisk/sfs/orm/support/{EntityMetadata,FieldMetadata,RelationMetadata,IdentifierGenerator}.java
git commit -m "feat(sfs-orm): EntityMetadata + FieldMetadata + RelationMetadata records — EntityMetadataAnalyzer 토대"
```

---

### Task B2: EntityMetadataAnalyzer (어노테이션 분석 + fail-fast 5종)

**Files:**
- Create: `sfs-orm/src/main/java/com/choisk/sfs/orm/support/EntityMetadataAnalyzer.java`
- Test: `sfs-orm/src/test/java/com/choisk/sfs/orm/support/EntityMetadataAnalyzerTest.java`

> **TDD 적용** (5종 검증 분기)

- [ ] **Step 1: 실패 테스트 작성 — 8건 (성공 1 + fail-fast 5종 + 캐싱 + 다중 등록)**

```java
package com.choisk.sfs.orm.support;

import com.choisk.sfs.orm.annotation.*;
import com.choisk.sfs.orm.annotation.SfsGeneratedValue.GenerationType;
import com.choisk.sfs.orm.annotation.SfsManyToOne.FetchType;
import com.choisk.sfs.orm.exception.SfsEntityMappingException;
import org.junit.jupiter.api.Test;
import static org.assertj.core.api.Assertions.*;

class EntityMetadataAnalyzerTest {
    
    @SfsEntity(name = "valid_users")
    static class ValidEntity {
        @SfsId @SfsGeneratedValue(strategy = GenerationType.IDENTITY) Long id;
        @SfsColumn String name;
    }
    
    @Test
    void analyzes_valid_entity_successfully() {
        EntityMetadata md = new EntityMetadataAnalyzer().analyze(ValidEntity.class);
        assertThat(md.tableName()).isEqualTo("valid_users");
        assertThat(md.idField().field().getName()).isEqualTo("id");
        assertThat(md.columns()).hasSize(1);
    }
    
    static class MissingEntityAnnotation {
        @SfsId Long id;
    }
    
    @Test
    void throws_when_class_not_annotated_with_SfsEntity() {
        assertThatThrownBy(() -> new EntityMetadataAnalyzer().analyze(MissingEntityAnnotation.class))
            .isInstanceOf(SfsEntityMappingException.class)
            .hasMessageContaining("@SfsEntity");
    }
    
    @SfsEntity static class NoIdField {
        @SfsColumn String name;
    }
    
    @Test
    void throws_when_no_SfsId_field() {
        assertThatThrownBy(() -> new EntityMetadataAnalyzer().analyze(NoIdField.class))
            .isInstanceOf(SfsEntityMappingException.class)
            .hasMessageContaining("@SfsId");
    }
    
    @SfsEntity static class TwoIdFields {
        @SfsId Long id1;
        @SfsId Long id2;
    }
    
    @Test
    void throws_when_multiple_SfsId_fields() {
        assertThatThrownBy(() -> new EntityMetadataAnalyzer().analyze(TwoIdFields.class))
            .isInstanceOf(SfsEntityMappingException.class)
            .hasMessageContaining("multiple");
    }
    
    @SfsEntity static class SequenceWithoutName {
        @SfsId @SfsGeneratedValue(strategy = GenerationType.SEQUENCE) Long id;
    }
    
    @Test
    void throws_when_SEQUENCE_strategy_missing_sequenceName() {
        assertThatThrownBy(() -> new EntityMetadataAnalyzer().analyze(SequenceWithoutName.class))
            .isInstanceOf(SfsEntityMappingException.class)
            .hasMessageContaining("sequenceName");
    }
    
    @SfsEntity static class ManyToOneWithoutEntityType {
        @SfsId Long id;
        @SfsManyToOne @SfsJoinColumn(name = "user_id") String notAnEntity;
    }
    
    @Test
    void throws_when_ManyToOne_target_not_SfsEntity() {
        assertThatThrownBy(() -> new EntityMetadataAnalyzer().analyze(ManyToOneWithoutEntityType.class))
            .isInstanceOf(SfsEntityMappingException.class)
            .hasMessageContaining("@SfsEntity");
    }
    
    @SfsEntity static class JoinColumnMissing {
        @SfsId Long id;
        @SfsManyToOne ValidEntity user;   // @SfsJoinColumn 누락
    }
    
    @Test
    void throws_when_ManyToOne_missing_JoinColumn() {
        assertThatThrownBy(() -> new EntityMetadataAnalyzer().analyze(JoinColumnMissing.class))
            .isInstanceOf(SfsEntityMappingException.class)
            .hasMessageContaining("@SfsJoinColumn");
    }
    
    @Test
    void caches_analyzed_metadata_per_class() {
        EntityMetadataAnalyzer analyzer = new EntityMetadataAnalyzer();
        EntityMetadata md1 = analyzer.analyze(ValidEntity.class);
        EntityMetadata md2 = analyzer.analyze(ValidEntity.class);
        assertThat(md1).isSameAs(md2);   // 동일 인스턴스 (캐싱)
    }
}
```

- [ ] **Step 2: FAIL 확인**

```
./gradlew :sfs-orm:test --tests EntityMetadataAnalyzerTest
```
Expected: 컴파일 에러 (`EntityMetadataAnalyzer` 미존재)

- [ ] **Step 3: 최소 구현 — `EntityMetadataAnalyzer.java`**

```java
package com.choisk.sfs.orm.support;

import com.choisk.sfs.orm.annotation.*;
import com.choisk.sfs.orm.annotation.SfsGeneratedValue.GenerationType;
import com.choisk.sfs.orm.exception.SfsEntityMappingException;

import java.lang.reflect.Field;
import java.util.*;
import java.util.concurrent.ConcurrentHashMap;
import java.util.stream.Collectors;

public class EntityMetadataAnalyzer {
    
    private final Map<Class<?>, EntityMetadata> cache = new ConcurrentHashMap<>();
    
    public EntityMetadata analyze(Class<?> entityClass) {
        return cache.computeIfAbsent(entityClass, this::doAnalyze);
    }
    
    private EntityMetadata doAnalyze(Class<?> entityClass) {
        SfsEntity entityAnno = entityClass.getAnnotation(SfsEntity.class);
        if (entityAnno == null) {
            throw new SfsEntityMappingException("Class " + entityClass.getName() + " is not annotated with @SfsEntity");
        }
        
        String tableName = entityAnno.name().isEmpty() ? entityClass.getSimpleName() : entityAnno.name();
        
        // @SfsId 필드 — 정확히 1개
        List<Field> idFields = Arrays.stream(entityClass.getDeclaredFields())
            .filter(f -> f.isAnnotationPresent(SfsId.class))
            .toList();
        if (idFields.isEmpty()) {
            throw new SfsEntityMappingException("Entity " + entityClass.getName() + " has no @SfsId field");
        }
        if (idFields.size() > 1) {
            throw new SfsEntityMappingException("Entity " + entityClass.getName() + " has multiple @SfsId fields (only one allowed)");
        }
        Field idField = idFields.get(0);
        idField.setAccessible(true);
        
        // IdentifierGenerator 결정 — Task E1/E2 후 구현, 일단 placeholder null
        IdentifierGenerator idGenerator = createIdGenerator(idField);
        
        FieldMetadata idMeta = new FieldMetadata(idField, columnNameOf(idField), idField.getType());
        
        // @SfsColumn 필드 (id field 제외) + @SfsManyToOne 필드
        List<FieldMetadata> columns = new ArrayList<>();
        List<RelationMetadata> manyToOnes = new ArrayList<>();
        for (Field f : entityClass.getDeclaredFields()) {
            f.setAccessible(true);
            if (f.equals(idField)) continue;
            if (f.isAnnotationPresent(SfsManyToOne.class)) {
                validateManyToOne(f);
                SfsManyToOne rel = f.getAnnotation(SfsManyToOne.class);
                SfsJoinColumn joinCol = f.getAnnotation(SfsJoinColumn.class);
                manyToOnes.add(new RelationMetadata(f, rel.fetch(), f.getType(), joinCol.name()));
            } else if (f.isAnnotationPresent(SfsColumn.class)) {
                columns.add(new FieldMetadata(f, columnNameOf(f), f.getType()));
            }
        }
        
        String insertSql = buildInsertSql(tableName, idField, columns, manyToOnes, idGenerator);
        String selectSql = buildSelectByIdSql(tableName, idField, columns, manyToOnes);
        String deleteSql = buildDeleteSql(tableName, idField);
        
        return new EntityMetadata(entityClass, tableName, idMeta, idGenerator, columns, manyToOnes,
                                   insertSql, selectSql, deleteSql);
    }
    
    private void validateManyToOne(Field f) {
        Class<?> targetType = f.getType();
        if (!targetType.isAnnotationPresent(SfsEntity.class)) {
            throw new SfsEntityMappingException("@SfsManyToOne field " + f.getName() + " target type " + targetType.getName() + " is not annotated with @SfsEntity");
        }
        if (!f.isAnnotationPresent(SfsJoinColumn.class)) {
            throw new SfsEntityMappingException("@SfsManyToOne field " + f.getName() + " missing @SfsJoinColumn");
        }
    }
    
    private IdentifierGenerator createIdGenerator(Field idField) {
        SfsGeneratedValue gv = idField.getAnnotation(SfsGeneratedValue.class);
        if (gv == null) {
            throw new SfsEntityMappingException("@SfsId field " + idField.getName() + " missing @SfsGeneratedValue");
        }
        if (gv.strategy() == GenerationType.SEQUENCE && gv.sequenceName().isEmpty()) {
            throw new SfsEntityMappingException("@SfsGeneratedValue(SEQUENCE) on " + idField.getName() + " missing sequenceName");
        }
        // E1/E2에서 실제 IdentityGenerator/SequenceGenerator 인스턴스 반환
        // 본 단계에서는 placeholder — analyzer가 jdbc를 모르므로 generator 생성을 builder가 담당하는 구조도 검토
        // 가장 단순: analyzer는 strategy만 record에 담고, factory가 jdbc 주입해서 generator 생성
        return null;   // ← E1/E2 통합 시 구조 정정 필요 — 하단 참고
    }
    
    private String columnNameOf(Field f) {
        SfsColumn col = f.getAnnotation(SfsColumn.class);
        if (col != null && !col.name().isEmpty()) return col.name();
        return f.getName();
    }
    
    private String buildInsertSql(String table, Field idField, List<FieldMetadata> cols, List<RelationMetadata> rels, IdentifierGenerator gen) {
        // IDENTITY: id 컬럼 제외, SEQUENCE/일반: id 포함
        boolean includeId = !(gen != null && gen.isPostInsert());
        List<String> colNames = new ArrayList<>();
        if (includeId) colNames.add(columnNameOf(idField));
        cols.forEach(c -> colNames.add(c.columnName()));
        rels.forEach(r -> colNames.add(r.joinColumnName()));
        String placeholders = colNames.stream().map(c -> "?").collect(Collectors.joining(", "));
        return "INSERT INTO " + table + " (" + String.join(", ", colNames) + ") VALUES (" + placeholders + ")";
    }
    
    private String buildSelectByIdSql(String table, Field idField, List<FieldMetadata> cols, List<RelationMetadata> rels) {
        List<String> colNames = new ArrayList<>();
        colNames.add(columnNameOf(idField));
        cols.forEach(c -> colNames.add(c.columnName()));
        rels.forEach(r -> colNames.add(r.joinColumnName()));
        return "SELECT " + String.join(", ", colNames) + " FROM " + table + " WHERE " + columnNameOf(idField) + " = ?";
    }
    
    private String buildDeleteSql(String table, Field idField) {
        return "DELETE FROM " + table + " WHERE " + columnNameOf(idField) + " = ?";
    }
}
```

> **구조 정정 필요 박제**: `EntityMetadataAnalyzer`가 단독으로 `IdentifierGenerator` 인스턴스를 생성하려면 `JdbcTemplate` 의존이 필요한데, analyzer는 *어노테이션 분석만* 책임지는 게 깔끔. 따라서 *generator 인스턴스 생성은 `SfsEntityManagerFactory.builder()`가 담당*하도록 구조 변경 필요. analyzer는 *generation strategy 정보만* 반환하고, factory가 jdbc를 주입해서 generator 생성. *Task E1/E2/C1 통합 시 구조 정정*.
> 
> 본 task는 fail-fast 5종 검증에 집중. `createIdGenerator`는 strategy/sequenceName 검증까지만 하고 null 반환. 통합 시 구조 정정 박제.

- [ ] **Step 4: 분석 결과를 record에 담는 형태로 구조 변경 — 임시 patch**

`EntityMetadata.java`에 `GenerationType strategy()`, `String sequenceName()` 추가하거나, 별도 `IdGeneratorSpec` record 신설:

```java
// support/IdGeneratorSpec.java (record)
public record IdGeneratorSpec(GenerationType strategy, String sequenceName) { }
```

`EntityMetadata`의 `idGenerator` 필드를 `IdGeneratorSpec idGeneratorSpec`로 교체:

```java
public record EntityMetadata(
    Class<?> entityClass,
    String tableName,
    FieldMetadata idField,
    IdGeneratorSpec idGeneratorSpec,   // ← 변경
    List<FieldMetadata> columns,
    List<RelationMetadata> manyToOnes,
    String insertSql,
    String selectByIdSql,
    String deleteSql
) { ... }
```

`EntityMetadataAnalyzer.createIdGenerator`를 `createIdGeneratorSpec`로 변경:

```java
private IdGeneratorSpec createIdGeneratorSpec(Field idField) {
    SfsGeneratedValue gv = idField.getAnnotation(SfsGeneratedValue.class);
    if (gv == null) {
        throw new SfsEntityMappingException("@SfsId field " + idField.getName() + " missing @SfsGeneratedValue");
    }
    if (gv.strategy() == GenerationType.SEQUENCE && gv.sequenceName().isEmpty()) {
        throw new SfsEntityMappingException("@SfsGeneratedValue(SEQUENCE) on " + idField.getName() + " missing sequenceName");
    }
    return new IdGeneratorSpec(gv.strategy(), gv.sequenceName());
}
```

`buildInsertSql`은 `IdGeneratorSpec`을 받도록 변경:
```java
boolean includeId = spec.strategy() != GenerationType.IDENTITY;
```

- [ ] **Step 5: PASS 확인**

```
./gradlew :sfs-orm:test --tests EntityMetadataAnalyzerTest
```
Expected: 8건 PASS / 0 FAIL

- [ ] **Step 6: 회귀 검증 + 커밋**

```
./gradlew test
```
Expected: 246 + 8 = 254 PASS / 0 FAIL

```bash
git add sfs-orm/src/main/java/com/choisk/sfs/orm/support/{EntityMetadata,EntityMetadataAnalyzer,IdGeneratorSpec}.java \
        sfs-orm/src/test/java/com/choisk/sfs/orm/support/EntityMetadataAnalyzerTest.java
git commit -m "feat(sfs-orm): EntityMetadataAnalyzer + fail-fast 5종 검증 + SQL 미리 생성 — EntityMetadata 토대 완성"
```

---

## 섹션 C: SfsEntityManager 인터페이스 + Factory builder

### Task C1: `SfsEntityManager` + `SfsEntityManagerFactory`

**Files:** Create:
- `sfs-orm/src/main/java/com/choisk/sfs/orm/SfsEntityManager.java`
- `sfs-orm/src/main/java/com/choisk/sfs/orm/SfsEntityManagerFactory.java`

> **TDD 제외** (인터페이스 시그니처 + builder는 통합 테스트가 회귀망)

- [ ] **Step 1: `SfsEntityManager` 인터페이스 작성**

```java
package com.choisk.sfs.orm;

public interface SfsEntityManager {
    void persist(Object entity);
    <T> T find(Class<T> entityClass, Object primaryKey);   // null이면 not found
    void remove(Object entity);
    void flush();
    <T> T merge(T entity);
    boolean contains(Object entity);
    // close()는 사용자가 호출하지 않음 — TSM 콜백에서 자동 (SfsEntityManagerFactoryBean)
}
```

- [ ] **Step 2: `SfsEntityManagerFactory` (skeleton — D1 이후 채움)**

```java
package com.choisk.sfs.orm;

import com.choisk.sfs.orm.support.*;
import com.choisk.sfs.tx.jdbc.JdbcTemplate;
import com.choisk.sfs.tx.support.TransactionSynchronizationManager;

import javax.sql.DataSource;
import java.util.*;

public class SfsEntityManagerFactory {
    
    private final DataSource dataSource;
    private final JdbcTemplate jdbcTemplate;
    private final TransactionSynchronizationManager tsm;
    private final Map<Class<?>, EntityMetadata> metadataByClass;
    private final Map<Class<?>, EntityPersister> persisterByClass;
    private final LazyProxyFactory lazyProxyFactory;
    
    private SfsEntityManagerFactory(Builder b) {
        this.dataSource = b.dataSource;
        this.tsm = b.tsm;
        this.jdbcTemplate = new JdbcTemplate(dataSource, tsm);
        this.lazyProxyFactory = new LazyProxyFactory();
        
        EntityMetadataAnalyzer analyzer = new EntityMetadataAnalyzer();
        this.metadataByClass = new HashMap<>();
        this.persisterByClass = new HashMap<>();
        for (Class<?> ec : b.entityClasses) {
            EntityMetadata md = analyzer.analyze(ec);
            metadataByClass.put(ec, md);
        }
        // EntityPersister는 generator를 필요로 하므로 generator 생성 후 persister 생성
        for (var entry : metadataByClass.entrySet()) {
            IdentifierGenerator gen = createGenerator(entry.getValue().idGeneratorSpec(), jdbcTemplate);
            EntityPersister persister = new EntityPersister(entry.getValue(), gen, jdbcTemplate);
            persisterByClass.put(entry.getKey(), persister);
        }
    }
    
    private static IdentifierGenerator createGenerator(IdGeneratorSpec spec, JdbcTemplate jdbc) {
        return switch (spec.strategy()) {
            case IDENTITY -> new IdentityGenerator(jdbc);
            case SEQUENCE -> new SequenceGenerator(spec.sequenceName(), jdbc);
        };
    }
    
    public SfsEntityManager createEntityManager() {
        return new RealEntityManager(this);
    }
    
    // package-private getters for RealEntityManager
    EntityMetadata metadataOf(Class<?> ec) { return metadataByClass.get(ec); }
    EntityPersister persisterOf(Class<?> ec) { return persisterByClass.get(ec); }
    LazyProxyFactory lazyProxyFactory() { return lazyProxyFactory; }
    DataSource dataSource() { return dataSource; }
    
    public static Builder builder() { return new Builder(); }
    
    public static class Builder {
        private DataSource dataSource;
        private TransactionSynchronizationManager tsm;
        private final List<Class<?>> entityClasses = new ArrayList<>();
        
        public Builder dataSource(DataSource ds) { this.dataSource = ds; return this; }
        public Builder transactionSynchronizationManager(TransactionSynchronizationManager tsm) { this.tsm = tsm; return this; }
        public Builder addEntityClass(Class<?> ec) { entityClasses.add(ec); return this; }
        
        public SfsEntityManagerFactory build() {
            Objects.requireNonNull(dataSource, "dataSource");
            Objects.requireNonNull(tsm, "transactionSynchronizationManager");
            return new SfsEntityManagerFactory(this);
        }
    }
}
```

> *D1 이전이라 `RealEntityManager`/`EntityPersister`/`LazyProxyFactory`/`IdentityGenerator`/`SequenceGenerator`는 stub. 임시 빈 클래스로 두고 D1~I2에서 채움.*

- [ ] **Step 3: 빈 stub 클래스 작성 — 컴파일만 통과시키기**

```java
// support/RealEntityManager.java (D1 이후 본격)
package com.choisk.sfs.orm.support;
import com.choisk.sfs.orm.SfsEntityManager;
import com.choisk.sfs.orm.SfsEntityManagerFactory;
public class RealEntityManager implements SfsEntityManager {
    public RealEntityManager(SfsEntityManagerFactory emf) { /* D1+ */ }
    @Override public void persist(Object entity) { throw new UnsupportedOperationException("G1+"); }
    @Override public <T> T find(Class<T> ec, Object pk) { throw new UnsupportedOperationException("H1+"); }
    @Override public void remove(Object e) { throw new UnsupportedOperationException("K1"); }
    @Override public void flush() { throw new UnsupportedOperationException("K2"); }
    @Override public <T> T merge(T e) { throw new UnsupportedOperationException("K3"); }
    @Override public boolean contains(Object e) { throw new UnsupportedOperationException("D1+"); }
}

// support/EntityPersister.java (F1 이후)
package com.choisk.sfs.orm.support;
import com.choisk.sfs.tx.jdbc.JdbcTemplate;
public class EntityPersister {
    public EntityPersister(EntityMetadata md, IdentifierGenerator gen, JdbcTemplate jdbc) { /* F1+ */ }
}

// support/LazyProxyFactory.java (I1)
package com.choisk.sfs.orm.support;
public class LazyProxyFactory { /* I1 */ }

// support/IdentityGenerator.java + SequenceGenerator.java (E1/E2)
package com.choisk.sfs.orm.support;
import com.choisk.sfs.tx.jdbc.JdbcTemplate;
public class IdentityGenerator implements IdentifierGenerator {
    public IdentityGenerator(JdbcTemplate jdbc) { /* E1 */ }
    public boolean isPostInsert() { return true; }
}
public class SequenceGenerator implements IdentifierGenerator {
    public SequenceGenerator(String name, JdbcTemplate jdbc) { /* E2 */ }
    public boolean isPostInsert() { return false; }
}
```

`IdentifierGenerator` 인터페이스 확장 (B1 stub에서):
```java
public interface IdentifierGenerator {
    boolean isPostInsert();
    // generate()는 E1/E2에서 추가
}
```

- [ ] **Step 4: 컴파일 + 회귀**

```
./gradlew :sfs-orm:compileJava :sfs-orm:test
```
Expected: 254 PASS (변동 없음)

- [ ] **Step 5: 커밋**

```bash
git add sfs-orm/src/main/java/com/choisk/sfs/orm/
git commit -m "feat(sfs-orm): SfsEntityManager 인터페이스 + Factory fluent builder + 컴포넌트 stub — 골격 완성"
```

---

## 섹션 D: PersistenceContext

### Task D1: PersistenceContext 자료구조 + 단위 테스트

**Files:**
- Create: `sfs-orm/src/main/java/com/choisk/sfs/orm/support/EntityKey.java`
- Create: `sfs-orm/src/main/java/com/choisk/sfs/orm/support/EntityAction.java`
- Create: `sfs-orm/src/main/java/com/choisk/sfs/orm/support/PersistenceContext.java`
- Test: `sfs-orm/src/test/java/com/choisk/sfs/orm/support/PersistenceContextTest.java`

> **TDD 적용** (분기 + 상태 + 라이프사이클)

- [ ] **Step 1: EntityKey + EntityAction sealed 신설**

```java
// EntityKey.java
package com.choisk.sfs.orm.support;
public record EntityKey(Class<?> entityClass, Object primaryKey) { }

// EntityAction.java
package com.choisk.sfs.orm.support;
import java.util.BitSet;

public sealed interface EntityAction permits InsertAction, DeleteAction, UpdateAction { 
    Object entity();
    EntityMetadata metadata();
}

record InsertAction(Object entity, EntityMetadata metadata) implements EntityAction { }
record DeleteAction(Object entity, EntityMetadata metadata) implements EntityAction { }
record UpdateAction(Object entity, EntityMetadata metadata, BitSet dirtyColumns) implements EntityAction { }
```

- [ ] **Step 2: 실패 테스트 작성 — 6건**

```java
package com.choisk.sfs.orm.support;

import org.junit.jupiter.api.Test;
import static org.assertj.core.api.Assertions.*;

class PersistenceContextTest {
    
    @Test
    void putEntity_then_get_returns_same_instance() {
        var ctx = new PersistenceContext();
        var key = new EntityKey(String.class, 1L);
        ctx.putEntity(key, "alice");
        assertThat(ctx.getEntity(key)).isSameAs("alice");
    }
    
    @Test
    void getEntity_returns_null_when_missing() {
        var ctx = new PersistenceContext();
        assertThat(ctx.getEntity(new EntityKey(String.class, 99L))).isNull();
    }
    
    @Test
    void putSnapshot_captures_field_values() {
        var ctx = new PersistenceContext();
        var key = new EntityKey(String.class, 1L);
        ctx.putSnapshot(key, new Object[]{"alice", "a@x.com"});
        assertThat(ctx.getSnapshot(key)).containsExactly("alice", "a@x.com");
    }
    
    @Test
    void enqueueAction_appends_to_queue_in_order() {
        var ctx = new PersistenceContext();
        ctx.enqueueAction(new InsertAction("a", null));
        ctx.enqueueAction(new InsertAction("b", null));
        assertThat(ctx.actionQueue()).hasSize(2);
        assertThat(ctx.actionQueue().get(0).entity()).isEqualTo("a");
    }
    
    @Test
    void close_marks_closed_and_clears_state() {
        var ctx = new PersistenceContext();
        ctx.putEntity(new EntityKey(String.class, 1L), "x");
        ctx.enqueueAction(new InsertAction("x", null));
        ctx.close();
        assertThat(ctx.isClosed()).isTrue();
        assertThat(ctx.getEntity(new EntityKey(String.class, 1L))).isNull();
        assertThat(ctx.actionQueue()).isEmpty();
    }
    
    @Test
    void contains_returns_true_for_managed_entity() {
        var ctx = new PersistenceContext();
        var key = new EntityKey(String.class, 1L);
        ctx.putEntity(key, "alice");
        assertThat(ctx.contains(key)).isTrue();
        assertThat(ctx.contains(new EntityKey(String.class, 99L))).isFalse();
    }
}
```

- [ ] **Step 3: FAIL 확인**

```
./gradlew :sfs-orm:test --tests PersistenceContextTest
```
Expected: 컴파일 에러

- [ ] **Step 4: 최소 구현**

```java
package com.choisk.sfs.orm.support;

import java.util.*;

public class PersistenceContext {
    
    private final Map<EntityKey, Object> identityMap = new LinkedHashMap<>();
    private final Map<EntityKey, Object[]> snapshots = new HashMap<>();
    private final List<EntityAction> actionQueue = new ArrayList<>();
    private boolean closed = false;
    
    public void putEntity(EntityKey key, Object entity) {
        ensureOpen();
        identityMap.put(key, entity);
    }
    
    public Object getEntity(EntityKey key) {
        if (closed) return null;
        return identityMap.get(key);
    }
    
    public boolean contains(EntityKey key) {
        return !closed && identityMap.containsKey(key);
    }
    
    public void putSnapshot(EntityKey key, Object[] values) {
        ensureOpen();
        snapshots.put(key, values);
    }
    
    public Object[] getSnapshot(EntityKey key) {
        return snapshots.get(key);
    }
    
    public void enqueueAction(EntityAction action) {
        ensureOpen();
        actionQueue.add(action);
    }
    
    public List<EntityAction> actionQueue() {
        return Collections.unmodifiableList(actionQueue);
    }
    
    public Map<EntityKey, Object> identityMap() {
        return Collections.unmodifiableMap(identityMap);
    }
    
    public void clearActionQueue() { actionQueue.clear(); }
    
    public boolean isClosed() { return closed; }
    
    public void close() {
        identityMap.clear();
        snapshots.clear();
        actionQueue.clear();
        closed = true;
    }
    
    private void ensureOpen() {
        if (closed) throw new IllegalStateException("PersistenceContext is closed");
    }
}
```

- [ ] **Step 5: PASS 확인 + 커밋**

```
./gradlew :sfs-orm:test --tests PersistenceContextTest
```
Expected: 6건 PASS

```bash
git add sfs-orm/src/main/java/com/choisk/sfs/orm/support/{EntityKey,EntityAction,PersistenceContext}.java \
        sfs-orm/src/test/java/com/choisk/sfs/orm/support/PersistenceContextTest.java
git commit -m "feat(sfs-orm): PersistenceContext + EntityKey + EntityAction sealed — 1차 캐시 + write-behind 큐 + snapshot"
```

회귀 254 + 6 = 260 PASS.

---

## 섹션 E: IdentifierGenerator 2종

### Task E1: `IdentifierGenerator` interface + `IdentityGenerator` (post-insert)

**Files:**
- Modify: `sfs-orm/src/main/java/com/choisk/sfs/orm/support/IdentifierGenerator.java`
- Modify: `sfs-orm/src/main/java/com/choisk/sfs/orm/support/IdentityGenerator.java`
- Test: `sfs-orm/src/test/java/com/choisk/sfs/orm/support/IdentityGeneratorTest.java`

- [ ] **Step 1: 인터페이스 본격 정의**

```java
// IdentifierGenerator.java
package com.choisk.sfs.orm.support;

public interface IdentifierGenerator {
    /** post-insert (IDENTITY)면 generate()가 INSERT까지 수행하고 generated key 반환. pre-insert (SEQUENCE)면 INSERT 전에 호출되어 ID만 반환. */
    Object generate(Object entity, EntityMetadata md);
    boolean isPostInsert();
}
```

- [ ] **Step 2: 실패 테스트 — IdentityGenerator 2건**

```java
package com.choisk.sfs.orm.support;

import com.choisk.sfs.orm.annotation.*;
import com.choisk.sfs.orm.annotation.SfsGeneratedValue.GenerationType;
import com.choisk.sfs.tx.jdbc.JdbcTemplate;
import com.choisk.sfs.tx.support.ThreadLocalTsm;
import org.h2.jdbcx.JdbcDataSource;
import org.junit.jupiter.api.*;
import static org.assertj.core.api.Assertions.*;

class IdentityGeneratorTest {
    
    private JdbcDataSource ds;
    private JdbcTemplate jdbc;
    
    @SfsEntity(name = "id_test")
    static class IdEntity {
        @SfsId @SfsGeneratedValue(strategy = GenerationType.IDENTITY) Long id;
        @SfsColumn String name;
    }
    
    @BeforeEach
    void setup() {
        ds = new JdbcDataSource();
        ds.setURL("jdbc:h2:mem:idtest;DB_CLOSE_DELAY=-1");
        jdbc = new JdbcTemplate(ds, new ThreadLocalTsm());
        jdbc.update("DROP TABLE IF EXISTS id_test");
        jdbc.update("CREATE TABLE id_test (id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, name VARCHAR(100))");
    }
    
    @Test
    void isPostInsert_returns_true() {
        var gen = new IdentityGenerator(jdbc);
        assertThat(gen.isPostInsert()).isTrue();
    }
    
    @Test
    void generate_executes_insert_and_returns_generated_key() throws Exception {
        var analyzer = new EntityMetadataAnalyzer();
        var md = analyzer.analyze(IdEntity.class);
        var gen = new IdentityGenerator(jdbc);
        
        IdEntity e = new IdEntity();
        e.name = "alice";
        
        Object key = gen.generate(e, md);
        
        assertThat(key).isNotNull();
        assertThat(((Number) key).longValue()).isEqualTo(1L);
        // DB에 행이 삽입됐는지 확인
        Long count = jdbc.queryForObject("SELECT COUNT(*) FROM id_test WHERE id = ?", Long.class, key);
        assertThat(count).isEqualTo(1L);
    }
}
```

- [ ] **Step 3: FAIL 확인**

```
./gradlew :sfs-orm:test --tests IdentityGeneratorTest
```
Expected: 컴파일 에러 (메서드 미존재)

- [ ] **Step 4: IdentityGenerator 구현**

```java
package com.choisk.sfs.orm.support;

import com.choisk.sfs.orm.exception.SfsPersistenceException;
import com.choisk.sfs.tx.jdbc.JdbcTemplate;

import java.lang.reflect.Field;
import java.util.ArrayList;
import java.util.List;

public class IdentityGenerator implements IdentifierGenerator {
    
    private final JdbcTemplate jdbc;
    
    public IdentityGenerator(JdbcTemplate jdbc) { this.jdbc = jdbc; }
    
    @Override public boolean isPostInsert() { return true; }
    
    @Override
    public Object generate(Object entity, EntityMetadata md) {
        // 학습 정점 박제: persist == INSERT (정점 ① 깨짐 함정)
        Object[] params = boundParams(entity, md);
        Number key = jdbc.updateAndReturnKey(md.insertSql(), params);
        try {
            md.idField().field().set(entity, convertKey(key, md.idField().javaType()));
        } catch (IllegalAccessException e) {
            throw new SfsPersistenceException("Cannot set @SfsId field after INSERT", e);
        }
        return key;
    }
    
    private Object convertKey(Number key, Class<?> idType) {
        if (idType == Long.class || idType == long.class) return key.longValue();
        if (idType == Integer.class || idType == int.class) return key.intValue();
        return key;
    }
    
    private Object[] boundParams(Object entity, EntityMetadata md) {
        // IDENTITY는 id 컬럼 제외, columns + relations FK 순서
        List<Object> params = new ArrayList<>();
        try {
            for (FieldMetadata col : md.columns()) {
                params.add(col.field().get(entity));
            }
            for (RelationMetadata rel : md.manyToOnes()) {
                Object related = rel.field().get(entity);
                params.add(related == null ? null : extractFk(related, rel.targetEntity()));
            }
        } catch (IllegalAccessException e) {
            throw new SfsPersistenceException("Cannot read entity fields", e);
        }
        return params.toArray();
    }
    
    private Object extractFk(Object related, Class<?> targetType) {
        // related entity의 @SfsId 필드값 — 임시로 reflection (analyzer 결과 캐싱은 추후)
        for (Field f : targetType.getDeclaredFields()) {
            if (f.isAnnotationPresent(com.choisk.sfs.orm.annotation.SfsId.class)) {
                f.setAccessible(true);
                try { return f.get(related); }
                catch (IllegalAccessException e) { throw new SfsPersistenceException("Cannot extract FK", e); }
            }
        }
        throw new SfsPersistenceException("Related entity has no @SfsId");
    }
}
```

- [ ] **Step 5: PASS 확인 + 커밋**

```
./gradlew :sfs-orm:test --tests IdentityGeneratorTest
```
Expected: 2건 PASS

```bash
git add sfs-orm/src/main/java/com/choisk/sfs/orm/support/{IdentifierGenerator,IdentityGenerator}.java \
        sfs-orm/src/test/java/com/choisk/sfs/orm/support/IdentityGeneratorTest.java
git commit -m "feat(sfs-orm): IdentifierGenerator interface + IdentityGenerator (post-insert) — 학습 정점 ① 깨짐 함정 박제"
```

회귀 260 + 2 = 262 PASS.

---

### Task E2: `SequenceGenerator` (pre-insert)

**Files:**
- Modify: `sfs-orm/src/main/java/com/choisk/sfs/orm/support/SequenceGenerator.java`
- Test: `sfs-orm/src/test/java/com/choisk/sfs/orm/support/SequenceGeneratorTest.java`

- [ ] **Step 1: 실패 테스트 — 2건**

```java
package com.choisk.sfs.orm.support;

import com.choisk.sfs.orm.annotation.*;
import com.choisk.sfs.orm.annotation.SfsGeneratedValue.GenerationType;
import com.choisk.sfs.tx.jdbc.JdbcTemplate;
import com.choisk.sfs.tx.support.ThreadLocalTsm;
import org.h2.jdbcx.JdbcDataSource;
import org.junit.jupiter.api.*;
import static org.assertj.core.api.Assertions.*;

class SequenceGeneratorTest {
    
    private JdbcDataSource ds;
    private JdbcTemplate jdbc;
    
    @SfsEntity(name = "seq_test")
    static class SeqEntity {
        @SfsId @SfsGeneratedValue(strategy = GenerationType.SEQUENCE, sequenceName = "seq_test_seq") Long id;
        @SfsColumn String name;
    }
    
    @BeforeEach
    void setup() {
        ds = new JdbcDataSource();
        ds.setURL("jdbc:h2:mem:seqtest;DB_CLOSE_DELAY=-1");
        jdbc = new JdbcTemplate(ds, new ThreadLocalTsm());
        jdbc.update("DROP SEQUENCE IF EXISTS seq_test_seq");
        jdbc.update("CREATE SEQUENCE seq_test_seq START WITH 1 INCREMENT BY 1");
    }
    
    @Test
    void isPostInsert_returns_false() {
        var gen = new SequenceGenerator("seq_test_seq", jdbc);
        assertThat(gen.isPostInsert()).isFalse();
    }
    
    @Test
    void generate_returns_next_sequence_value_without_inserting() {
        var analyzer = new EntityMetadataAnalyzer();
        var md = analyzer.analyze(SeqEntity.class);
        var gen = new SequenceGenerator("seq_test_seq", jdbc);
        
        SeqEntity e = new SeqEntity();
        e.name = "alice";
        
        Object key1 = gen.generate(e, md);
        Object key2 = gen.generate(e, md);
        
        assertThat(((Number) key1).longValue()).isEqualTo(1L);
        assertThat(((Number) key2).longValue()).isEqualTo(2L);
        // INSERT는 발생하지 않아야 함 — 학습 정점 ① 정상 박제
    }
}
```

- [ ] **Step 2: FAIL 확인**

- [ ] **Step 3: 구현**

```java
package com.choisk.sfs.orm.support;

import com.choisk.sfs.tx.jdbc.JdbcTemplate;

public class SequenceGenerator implements IdentifierGenerator {
    
    private final String sequenceName;
    private final JdbcTemplate jdbc;
    
    public SequenceGenerator(String sequenceName, JdbcTemplate jdbc) {
        this.sequenceName = sequenceName;
        this.jdbc = jdbc;
    }
    
    @Override public boolean isPostInsert() { return false; }
    
    @Override
    public Object generate(Object entity, EntityMetadata md) {
        return jdbc.queryForObject("SELECT NEXTVAL('" + sequenceName + "')", Long.class);
    }
}
```

- [ ] **Step 4: PASS 확인 + 커밋**

```bash
git add sfs-orm/src/main/java/com/choisk/sfs/orm/support/SequenceGenerator.java \
        sfs-orm/src/test/java/com/choisk/sfs/orm/support/SequenceGeneratorTest.java
git commit -m "feat(sfs-orm): SequenceGenerator (pre-insert, 매번 DB 호출) — 학습 정점 ① 정상 박제"
```

회귀 262 + 2 = 264 PASS.

---

## 섹션 F: EntityPersister

### Task F1: `EntityPersister` — INSERT/SELECT/DELETE SQL 실행

**Files:**
- Modify: `sfs-orm/src/main/java/com/choisk/sfs/orm/support/EntityPersister.java`
- Test: `sfs-orm/src/test/java/com/choisk/sfs/orm/support/EntityPersisterTest.java`

- [ ] **Step 1: 실패 테스트 — 3건 (INSERT/SELECT/DELETE)**

```java
package com.choisk.sfs.orm.support;

import com.choisk.sfs.orm.annotation.*;
import com.choisk.sfs.orm.annotation.SfsGeneratedValue.GenerationType;
import com.choisk.sfs.tx.jdbc.JdbcTemplate;
import com.choisk.sfs.tx.support.ThreadLocalTsm;
import org.h2.jdbcx.JdbcDataSource;
import org.junit.jupiter.api.*;
import static org.assertj.core.api.Assertions.*;

class EntityPersisterTest {
    
    private JdbcDataSource ds;
    private JdbcTemplate jdbc;
    private EntityMetadata md;
    private SequenceGenerator gen;
    private EntityPersister persister;
    
    @SfsEntity(name = "epersister_test")
    static class TestEntity {
        @SfsId @SfsGeneratedValue(strategy = GenerationType.SEQUENCE, sequenceName = "ep_seq") Long id;
        @SfsColumn String name;
        @SfsColumn(name = "age_value") int age;
    }
    
    @BeforeEach
    void setup() {
        ds = new JdbcDataSource();
        ds.setURL("jdbc:h2:mem:epersister;DB_CLOSE_DELAY=-1");
        jdbc = new JdbcTemplate(ds, new ThreadLocalTsm());
        jdbc.update("DROP TABLE IF EXISTS epersister_test");
        jdbc.update("DROP SEQUENCE IF EXISTS ep_seq");
        jdbc.update("CREATE SEQUENCE ep_seq START WITH 1");
        jdbc.update("CREATE TABLE epersister_test (id BIGINT PRIMARY KEY, name VARCHAR(100), age_value INT)");
        md = new EntityMetadataAnalyzer().analyze(TestEntity.class);
        gen = new SequenceGenerator("ep_seq", jdbc);
        persister = new EntityPersister(md, gen, jdbc);
    }
    
    @Test
    void executeInsert_persists_row_to_db() {
        TestEntity e = new TestEntity();
        e.id = 1L;
        e.name = "alice";
        e.age = 30;
        persister.executeInsert(e);
        
        Long count = jdbc.queryForObject("SELECT COUNT(*) FROM epersister_test WHERE id = 1 AND name = 'alice' AND age_value = 30", Long.class);
        assertThat(count).isEqualTo(1L);
    }
    
    @Test
    void loadById_reads_row_and_maps_columns() {
        jdbc.update("INSERT INTO epersister_test VALUES (?, ?, ?)", 5L, "bob", 25);
        Object loaded = persister.loadById(5L, null /* PersistenceContext - lazy 없음 */);
        assertThat(loaded).isInstanceOf(TestEntity.class);
        TestEntity t = (TestEntity) loaded;
        assertThat(t.id).isEqualTo(5L);
        assertThat(t.name).isEqualTo("bob");
        assertThat(t.age).isEqualTo(25);
    }
    
    @Test
    void executeDelete_removes_row() {
        jdbc.update("INSERT INTO epersister_test VALUES (?, ?, ?)", 7L, "x", 1);
        TestEntity e = new TestEntity(); e.id = 7L;
        persister.executeDelete(e);
        Long count = jdbc.queryForObject("SELECT COUNT(*) FROM epersister_test WHERE id = 7", Long.class);
        assertThat(count).isEqualTo(0L);
    }
}
```

- [ ] **Step 2: FAIL 확인**

- [ ] **Step 3: `EntityPersister` 구현 (F1 분량)**

```java
package com.choisk.sfs.orm.support;

import com.choisk.sfs.orm.exception.SfsPersistenceException;
import com.choisk.sfs.tx.jdbc.JdbcTemplate;
import com.choisk.sfs.tx.jdbc.RowMapper;

import java.lang.reflect.Field;
import java.util.*;

public class EntityPersister {
    
    private final EntityMetadata md;
    private final IdentifierGenerator idGenerator;
    private final JdbcTemplate jdbc;
    
    public EntityPersister(EntityMetadata md, IdentifierGenerator idGenerator, JdbcTemplate jdbc) {
        this.md = md;
        this.idGenerator = idGenerator;
        this.jdbc = jdbc;
    }
    
    public IdentifierGenerator idGenerator() { return idGenerator; }
    public EntityMetadata metadata() { return md; }
    
    public Object loadById(Object pk, PersistenceContext context) {
        List<Object> rows = jdbc.query(md.selectByIdSql(), buildRowMapper(context), pk);
        if (rows.isEmpty()) return null;
        return rows.get(0);
    }
    
    public void executeInsert(Object entity) {
        Object[] params = buildInsertParams(entity);
        jdbc.update(md.insertSql(), params);
    }
    
    public void executeDelete(Object entity) {
        try {
            Object pk = md.idField().field().get(entity);
            jdbc.update(md.deleteSql(), pk);
        } catch (IllegalAccessException e) {
            throw new SfsPersistenceException("Cannot read @SfsId for delete", e);
        }
    }
    
    private Object[] buildInsertParams(Object entity) {
        List<Object> params = new ArrayList<>();
        try {
            // IDENTITY 전략은 id 제외 (idGenerator.isPostInsert == true)
            if (!idGenerator.isPostInsert()) {
                params.add(md.idField().field().get(entity));
            }
            for (FieldMetadata col : md.columns()) params.add(col.field().get(entity));
            for (RelationMetadata rel : md.manyToOnes()) {
                Object related = rel.field().get(entity);
                params.add(related == null ? null : extractFk(related, rel.targetEntity()));
            }
        } catch (IllegalAccessException e) {
            throw new SfsPersistenceException("Cannot read entity fields", e);
        }
        return params.toArray();
    }
    
    private Object extractFk(Object related, Class<?> targetType) {
        for (Field f : targetType.getDeclaredFields()) {
            if (f.isAnnotationPresent(com.choisk.sfs.orm.annotation.SfsId.class)) {
                f.setAccessible(true);
                try { return f.get(related); }
                catch (IllegalAccessException e) { throw new SfsPersistenceException("FK 추출 실패", e); }
            }
        }
        throw new SfsPersistenceException("@SfsId 누락");
    }
    
    private RowMapper<Object> buildRowMapper(PersistenceContext context) {
        return (rs, rowNum) -> {
            try {
                Object instance = md.entityClass().getDeclaredConstructor().newInstance();
                int idx = 1;
                md.idField().field().set(instance, rs.getObject(idx++, md.idField().javaType()));
                for (FieldMetadata col : md.columns()) {
                    col.field().set(instance, rs.getObject(idx++, col.javaType()));
                }
                for (RelationMetadata rel : md.manyToOnes()) {
                    Object fk = rs.getObject(idx++);
                    // J1 (다음 task)에서 LAZY/EAGER 분기 + lazy proxy 채우기 추가
                    // 현재는 fk 무시 (Task J1까지 lazy 미처리)
                    // 임시: null 그대로 두되 J1에서 정정
                }
                return instance;
            } catch (Exception e) {
                throw new SfsPersistenceException("Row mapping 실패: " + md.entityClass().getName(), e);
            }
        };
    }
}
```

- [ ] **Step 4: PASS 확인 + 커밋**

```bash
git add sfs-orm/src/main/java/com/choisk/sfs/orm/support/EntityPersister.java \
        sfs-orm/src/test/java/com/choisk/sfs/orm/support/EntityPersisterTest.java
git commit -m "feat(sfs-orm): EntityPersister INSERT/SELECT/DELETE — SQL 캐싱 + reflection 매핑"
```

회귀 264 + 3 = 267 PASS.

---

### Task F2: `EntityPersister.executeUpdate` — 동적 SET 절 (dirty 컬럼 BitSet)

**Files:**
- Modify: `sfs-orm/src/main/java/com/choisk/sfs/orm/support/EntityPersister.java`
- Modify: `sfs-orm/src/test/java/com/choisk/sfs/orm/support/EntityPersisterTest.java`

- [ ] **Step 1: 실패 테스트 — 2건 (단일 dirty / 다중 dirty)**

```java
@Test
void executeUpdate_with_single_dirty_column_generates_correct_sql() {
    jdbc.update("INSERT INTO epersister_test VALUES (?, ?, ?)", 10L, "old", 20);
    TestEntity e = new TestEntity();
    e.id = 10L; e.name = "new_name"; e.age = 20;
    
    BitSet dirty = new BitSet(); dirty.set(0);   // 0번 컬럼만 (name)
    persister.executeUpdate(e, dirty);
    
    String name = jdbc.queryForObject("SELECT name FROM epersister_test WHERE id = 10", String.class);
    Integer age = jdbc.queryForObject("SELECT age_value FROM epersister_test WHERE id = 10", Integer.class);
    assertThat(name).isEqualTo("new_name");
    assertThat(age).isEqualTo(20);   // age는 변경 안 됨
}

@Test
void executeUpdate_with_multiple_dirty_columns_generates_correct_sql() {
    jdbc.update("INSERT INTO epersister_test VALUES (?, ?, ?)", 11L, "old", 30);
    TestEntity e = new TestEntity();
    e.id = 11L; e.name = "n2"; e.age = 99;
    
    BitSet dirty = new BitSet(); dirty.set(0); dirty.set(1);
    persister.executeUpdate(e, dirty);
    
    String name = jdbc.queryForObject("SELECT name FROM epersister_test WHERE id = 11", String.class);
    Integer age = jdbc.queryForObject("SELECT age_value FROM epersister_test WHERE id = 11", Integer.class);
    assertThat(name).isEqualTo("n2");
    assertThat(age).isEqualTo(99);
}
```

- [ ] **Step 2: FAIL 확인**

- [ ] **Step 3: `executeUpdate` 추가**

```java
public void executeUpdate(Object entity, BitSet dirtyColumns) {
    if (dirtyColumns.isEmpty()) return;   // no-op
    
    List<String> setClauses = new ArrayList<>();
    List<Object> params = new ArrayList<>();
    int colIdx = 0;
    try {
        for (FieldMetadata col : md.columns()) {
            if (dirtyColumns.get(colIdx)) {
                setClauses.add(col.columnName() + " = ?");
                params.add(col.field().get(entity));
            }
            colIdx++;
        }
        for (RelationMetadata rel : md.manyToOnes()) {
            if (dirtyColumns.get(colIdx)) {
                setClauses.add(rel.joinColumnName() + " = ?");
                Object related = rel.field().get(entity);
                params.add(related == null ? null : extractFk(related, rel.targetEntity()));
            }
            colIdx++;
        }
        params.add(md.idField().field().get(entity));
    } catch (IllegalAccessException e) {
        throw new SfsPersistenceException("Cannot read entity for UPDATE", e);
    }
    
    String sql = "UPDATE " + md.tableName() + " SET " + String.join(", ", setClauses)
               + " WHERE " + md.idField().columnName() + " = ?";
    jdbc.update(sql, params.toArray());
}
```

- [ ] **Step 4: PASS 확인 + 커밋**

```bash
git add sfs-orm/src/main/java/com/choisk/sfs/orm/support/EntityPersister.java \
        sfs-orm/src/test/java/com/choisk/sfs/orm/support/EntityPersisterTest.java
git commit -m "feat(sfs-orm): EntityPersister.executeUpdate — dirty 컬럼 BitSet 기반 동적 SET (Hibernate dynamic-update 패턴)"
```

회귀 267 + 2 = 269 PASS.

---

## 섹션 G+H: RealEntityManager (persist + find)

### Task G1: `RealEntityManager.persist()` — SEQUENCE 분기 (write-behind)

**Files:**
- Modify: `sfs-orm/src/main/java/com/choisk/sfs/orm/support/RealEntityManager.java`
- Test: `sfs-orm/src/test/java/com/choisk/sfs/orm/support/RealEntityManagerPersistTest.java` (신설)

- [ ] **Step 1: 실패 테스트 — SEQUENCE persist는 actionQueue에 등록되고 INSERT 미발생**

```java
package com.choisk.sfs.orm.support;

import com.choisk.sfs.orm.SfsEntityManagerFactory;
import com.choisk.sfs.orm.annotation.*;
import com.choisk.sfs.orm.annotation.SfsGeneratedValue.GenerationType;
import com.choisk.sfs.tx.jdbc.JdbcTemplate;
import com.choisk.sfs.tx.support.ThreadLocalTsm;
import org.h2.jdbcx.JdbcDataSource;
import org.junit.jupiter.api.*;
import static org.assertj.core.api.Assertions.*;

class RealEntityManagerPersistTest {
    
    @SfsEntity(name = "persist_seq_test")
    static class SeqUser {
        @SfsId @SfsGeneratedValue(strategy = GenerationType.SEQUENCE, sequenceName = "persist_seq") Long id;
        @SfsColumn String name;
    }
    
    private SfsEntityManagerFactory emf;
    private JdbcTemplate jdbc;
    
    @BeforeEach
    void setup() {
        var ds = new JdbcDataSource();
        ds.setURL("jdbc:h2:mem:rempersist;DB_CLOSE_DELAY=-1");
        var tsm = new ThreadLocalTsm();
        jdbc = new JdbcTemplate(ds, tsm);
        jdbc.update("DROP TABLE IF EXISTS persist_seq_test");
        jdbc.update("DROP SEQUENCE IF EXISTS persist_seq");
        jdbc.update("CREATE SEQUENCE persist_seq START WITH 1");
        jdbc.update("CREATE TABLE persist_seq_test (id BIGINT PRIMARY KEY, name VARCHAR(100))");
        emf = SfsEntityManagerFactory.builder()
            .dataSource(ds).transactionSynchronizationManager(tsm)
            .addEntityClass(SeqUser.class)
            .build();
    }
    
    @Test
    void persist_with_SEQUENCE_strategy_assigns_id_but_does_not_insert() {
        var em = (RealEntityManager) emf.createEntityManager();
        SeqUser u = new SeqUser(); u.name = "alice";
        
        em.persist(u);
        
        assertThat(u.id).isEqualTo(1L);   // SEQUENCE 미리 받음
        // DB에는 아직 INSERT 안 됨 (학습 정점 ① 정상 박제)
        Long count = jdbc.queryForObject("SELECT COUNT(*) FROM persist_seq_test", Long.class);
        assertThat(count).isEqualTo(0L);
        // 1차 캐시에 등재
        assertThat(em.context().contains(new EntityKey(SeqUser.class, 1L))).isTrue();
        // actionQueue에 InsertAction 추가
        assertThat(em.context().actionQueue()).hasSize(1);
        assertThat(em.context().actionQueue().get(0)).isInstanceOf(InsertAction.class);
    }
}
```

- [ ] **Step 2: FAIL 확인**

- [ ] **Step 3: `RealEntityManager.persist()` SEQUENCE 분기 구현**

```java
package com.choisk.sfs.orm.support;

import com.choisk.sfs.orm.SfsEntityManager;
import com.choisk.sfs.orm.SfsEntityManagerFactory;
import com.choisk.sfs.orm.exception.SfsPersistenceException;

public class RealEntityManager implements SfsEntityManager {
    
    private final SfsEntityManagerFactory emf;
    private final PersistenceContext context = new PersistenceContext();
    
    public RealEntityManager(SfsEntityManagerFactory emf) { this.emf = emf; }
    
    public PersistenceContext context() { return context; }
    
    @Override
    public void persist(Object entity) {
        EntityMetadata md = emf.metadataOf(entity.getClass());
        if (md == null) throw new SfsPersistenceException("Unknown entity class: " + entity.getClass());
        
        EntityPersister persister = emf.persisterOf(entity.getClass());
        IdentifierGenerator gen = persister.idGenerator();
        
        if (gen.isPostInsert()) {
            // IDENTITY: G2에서 처리
            throw new UnsupportedOperationException("IDENTITY 전략은 G2에서 구현");
        } else {
            // SEQUENCE: pre-insert
            Object id = gen.generate(entity, md);
            try {
                md.idField().field().set(entity, convertId(id, md.idField().javaType()));
            } catch (IllegalAccessException e) {
                throw new SfsPersistenceException("Cannot set @SfsId field", e);
            }
            EntityKey key = new EntityKey(entity.getClass(), md.idField().field().getLong(entity));
            context.putEntity(key, entity);
            context.putSnapshot(key, captureSnapshot(entity, md));
            context.enqueueAction(new InsertAction(entity, md));
        }
    }
    
    private Object convertId(Object id, Class<?> idType) {
        if (id instanceof Number n) {
            if (idType == Long.class || idType == long.class) return n.longValue();
            if (idType == Integer.class || idType == int.class) return n.intValue();
        }
        return id;
    }
    
    private Object[] captureSnapshot(Object entity, EntityMetadata md) {
        Object[] snap = new Object[md.columns().size() + md.manyToOnes().size()];
        int idx = 0;
        try {
            for (FieldMetadata col : md.columns()) snap[idx++] = col.field().get(entity);
            for (RelationMetadata rel : md.manyToOnes()) snap[idx++] = rel.field().get(entity);
        } catch (IllegalAccessException e) {
            throw new SfsPersistenceException("Cannot capture snapshot", e);
        }
        return snap;
    }
    
    @Override public <T> T find(Class<T> ec, Object pk) { throw new UnsupportedOperationException("H1"); }
    @Override public void remove(Object e) { throw new UnsupportedOperationException("K1"); }
    @Override public void flush() { throw new UnsupportedOperationException("K2"); }
    @Override public <T> T merge(T e) { throw new UnsupportedOperationException("K3"); }
    @Override public boolean contains(Object e) {
        EntityMetadata md = emf.metadataOf(e.getClass());
        try {
            return context.contains(new EntityKey(e.getClass(), md.idField().field().get(e)));
        } catch (IllegalAccessException ex) {
            return false;
        }
    }
}
```

> *`md.idField().field().getLong(entity)` 사용 시 idType이 Long이라는 가정. 안전하게 `(Long)md.idField().field().get(entity)` 또는 reflection 헬퍼.*

- [ ] **Step 4: PASS 확인 + 커밋**

```bash
git add sfs-orm/src/main/java/com/choisk/sfs/orm/support/RealEntityManager.java \
        sfs-orm/src/test/java/com/choisk/sfs/orm/support/RealEntityManagerPersistTest.java
git commit -m "feat(sfs-orm): RealEntityManager.persist() SEQUENCE 분기 — write-behind 박제 (학습 정점 ① 정상)"
```

회귀 269 + 1 = 270 PASS.

---

### Task G2: `RealEntityManager.persist()` — IDENTITY 분기 (즉시 INSERT)

**Files:** Modify both above.

- [ ] **Step 1: 실패 테스트 — IDENTITY persist는 즉시 INSERT, actionQueue에 추가 안 됨**

```java
@SfsEntity(name = "persist_id_test")
static class IdOrder {
    @SfsId @SfsGeneratedValue(strategy = GenerationType.IDENTITY) Long id;
    @SfsColumn String status;
}

@Test
void persist_with_IDENTITY_strategy_immediately_inserts_and_assigns_id() {
    jdbc.update("CREATE TABLE persist_id_test (id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, status VARCHAR(50))");
    var emf2 = SfsEntityManagerFactory.builder()
        .dataSource(ds).transactionSynchronizationManager(tsm)
        .addEntityClass(IdOrder.class)
        .build();
    var em = (RealEntityManager) emf2.createEntityManager();
    
    IdOrder o = new IdOrder(); o.status = "PENDING";
    em.persist(o);
    
    assertThat(o.id).isEqualTo(1L);   // generated key
    // DB에 즉시 INSERT (학습 정점 ① 깨짐 박제)
    Long count = jdbc.queryForObject("SELECT COUNT(*) FROM persist_id_test", Long.class);
    assertThat(count).isEqualTo(1L);
    // actionQueue에는 추가 안 됨
    assertThat(em.context().actionQueue()).isEmpty();
    // 1차 캐시에는 등재
    assertThat(em.context().contains(new EntityKey(IdOrder.class, 1L))).isTrue();
}
```

> *test setup 헬퍼 — `ds`/`tsm`을 클래스 필드로 추출 또는 setup 메서드 분리.*

- [ ] **Step 2: FAIL 확인 (`UnsupportedOperationException`)**

- [ ] **Step 3: IDENTITY 분기 구현**

`RealEntityManager.persist()`의 `if (gen.isPostInsert())` 분기 채우기:

```java
if (gen.isPostInsert()) {
    // IDENTITY: 즉시 INSERT (학습 정점 ① 깨짐 박제)
    Object id = gen.generate(entity, md);   // INSERT 수행 + entity.id 채움
    EntityKey key = new EntityKey(entity.getClass(), id);
    context.putEntity(key, entity);
    context.putSnapshot(key, captureSnapshot(entity, md));
    // actionQueue 추가 X (이미 INSERT됨)
}
```

- [ ] **Step 4: PASS 확인 + 커밋**

```bash
git add sfs-orm/src/main/java/com/choisk/sfs/orm/support/RealEntityManager.java \
        sfs-orm/src/test/java/com/choisk/sfs/orm/support/RealEntityManagerPersistTest.java
git commit -m "feat(sfs-orm): RealEntityManager.persist() IDENTITY 분기 — 즉시 INSERT (학습 정점 ① 깨짐 함정 박제)"
```

회귀 270 + 1 = 271 PASS.

---

### Task H1: `RealEntityManager.find()` — cache hit + miss SELECT + 컬럼 매핑

**Files:** Modify `RealEntityManager.java`.

> 단위 테스트는 통합 테스트(M1 BasicCrud)가 회귀망 — 본 task는 *구현만* 하고 단위 테스트는 생략. 회귀 +0.

- [ ] **Step 1: `find()` 본격 구현**

```java
@Override
public <T> T find(Class<T> entityClass, Object primaryKey) {
    EntityMetadata md = emf.metadataOf(entityClass);
    if (md == null) throw new SfsPersistenceException("Unknown entity class: " + entityClass);
    
    EntityKey key = new EntityKey(entityClass, primaryKey);
    
    // 1차 캐시 hit
    Object cached = context.getEntity(key);
    if (cached != null) return entityClass.cast(cached);
    
    // cache miss → DB 조회
    EntityPersister persister = emf.persisterOf(entityClass);
    Object loaded = persister.loadById(primaryKey, context);
    if (loaded == null) return null;   // not found
    
    context.putEntity(key, loaded);
    context.putSnapshot(key, captureSnapshot(loaded, md));
    return entityClass.cast(loaded);
}
```

- [ ] **Step 2: 컴파일 + 회귀 (기존 테스트 변동 없음)**

```
./gradlew :sfs-orm:test
```
Expected: 271 PASS (변동 없음)

- [ ] **Step 3: 커밋**

```bash
git add sfs-orm/src/main/java/com/choisk/sfs/orm/support/RealEntityManager.java
git commit -m "feat(sfs-orm): RealEntityManager.find() — 1차 캐시 hit/miss + DB 조회 (lazy 채우기는 J1)"
```

회귀 271 PASS.

---

## 섹션 I: LazyProxyFactory + LazyInterceptor

### Task I1: `LazyProxyFactory` — byte-buddy 서브클래싱

**Files:**
- Modify: `sfs-orm/src/main/java/com/choisk/sfs/orm/support/LazyProxyFactory.java`
- Test: `sfs-orm/src/test/java/com/choisk/sfs/orm/support/LazyProxyFactoryTest.java`

- [ ] **Step 1: 실패 테스트 — 3건 (생성 / caching / getId 비-인터셉트)**

```java
package com.choisk.sfs.orm.support;

import com.choisk.sfs.orm.annotation.*;
import com.choisk.sfs.orm.annotation.SfsGeneratedValue.GenerationType;
import org.junit.jupiter.api.Test;
import static org.assertj.core.api.Assertions.*;

class LazyProxyFactoryTest {
    
    @SfsEntity static class LazyTarget {
        @SfsId @SfsGeneratedValue(strategy = GenerationType.IDENTITY) Long id;
        @SfsColumn String name;
        public Long getId() { return id; }
        public String getName() { return name; }
    }
    
    @Test
    void createProxy_returns_subclass_instance_of_target() {
        var factory = new LazyProxyFactory();
        Object proxy = factory.createProxy(LazyTarget.class, 1L, new PersistenceContext());
        assertThat(proxy).isInstanceOf(LazyTarget.class);
        assertThat(proxy.getClass()).isNotEqualTo(LazyTarget.class);   // 서브클래스
    }
    
    @Test
    void createProxy_caches_enhanced_class_per_target() {
        var factory = new LazyProxyFactory();
        Object p1 = factory.createProxy(LazyTarget.class, 1L, new PersistenceContext());
        Object p2 = factory.createProxy(LazyTarget.class, 2L, new PersistenceContext());
        assertThat(p1.getClass()).isSameAs(p2.getClass());   // 같은 enhanced 클래스
    }
    
    @Test
    void getId_does_not_trigger_initialization() {
        var factory = new LazyProxyFactory();
        var ctx = new PersistenceContext();
        ctx.close();   // closed 영속성 컨텍스트 — getId 외 호출 시 예외
        
        Object proxy = factory.createProxy(LazyTarget.class, 42L, ctx);
        // getId()는 hidden field에서 즉시 반환 (인터셉트 안 됨)
        Long id = ((LazyTarget) proxy).getId();
        assertThat(id).isEqualTo(42L);
    }
}
```

- [ ] **Step 2: FAIL 확인**

- [ ] **Step 3: `LazyProxyFactory` 구현**

```java
package com.choisk.sfs.orm.support;

import com.choisk.sfs.orm.exception.SfsPersistenceException;
import net.bytebuddy.ByteBuddy;
import net.bytebuddy.description.modifier.Visibility;
import net.bytebuddy.implementation.FieldAccessor;
import net.bytebuddy.implementation.MethodDelegation;
import net.bytebuddy.matcher.ElementMatchers;

import java.lang.reflect.Constructor;
import java.lang.reflect.Field;
import java.util.Map;
import java.util.concurrent.ConcurrentHashMap;

public class LazyProxyFactory {
    
    public static final String INTERCEPTOR_FIELD = "$$lazyInterceptor";
    public static final String PK_FIELD = "$$lazyPk";
    
    private final Map<Class<?>, Class<?>> enhancedCache = new ConcurrentHashMap<>();
    
    public Object createProxy(Class<?> targetClass, Object pk, PersistenceContext context) {
        Class<?> enhanced = enhancedCache.computeIfAbsent(targetClass, this::buildEnhanced);
        try {
            Constructor<?> ctor = enhanced.getDeclaredConstructor();
            ctor.setAccessible(true);
            Object proxy = ctor.newInstance();
            
            Field interceptorField = enhanced.getDeclaredField(INTERCEPTOR_FIELD);
            interceptorField.setAccessible(true);
            interceptorField.set(proxy, new LazyInterceptor(targetClass, pk, context));
            
            Field pkField = enhanced.getDeclaredField(PK_FIELD);
            pkField.setAccessible(true);
            pkField.set(proxy, pk);
            
            // PK getter가 hidden 필드에서 바로 반환하도록 id 필드도 채움
            Field idField = findIdField(targetClass);
            idField.setAccessible(true);
            idField.set(proxy, pk);
            
            return proxy;
        } catch (Exception e) {
            throw new SfsPersistenceException("Lazy proxy 생성 실패: " + targetClass.getName(), e);
        }
    }
    
    private Class<?> buildEnhanced(Class<?> targetClass) {
        Field idField = findIdField(targetClass);
        return new ByteBuddy()
            .subclass(targetClass)
            .defineField(INTERCEPTOR_FIELD, LazyInterceptor.class, Visibility.PRIVATE)
            .defineField(PK_FIELD, Object.class, Visibility.PRIVATE)
            // getId 같은 PK getter는 인터셉트 제외 (lazy init 회피)
            .method(ElementMatchers.isPublic()
                .and(ElementMatchers.not(ElementMatchers.named("get" + capitalize(idField.getName()))))
                .and(ElementMatchers.not(ElementMatchers.isDeclaredBy(Object.class))))
            .intercept(MethodDelegation.toField(INTERCEPTOR_FIELD))
            .make()
            .load(targetClass.getClassLoader())
            .getLoaded();
    }
    
    private Field findIdField(Class<?> targetClass) {
        for (Field f : targetClass.getDeclaredFields()) {
            if (f.isAnnotationPresent(com.choisk.sfs.orm.annotation.SfsId.class)) {
                return f;
            }
        }
        throw new SfsPersistenceException("@SfsId 누락: " + targetClass.getName());
    }
    
    private String capitalize(String s) {
        return s.substring(0, 1).toUpperCase() + s.substring(1);
    }
}
```

> *byte-buddy `MethodDelegation.toField`는 필드에 저장된 인터셉터 인스턴스로 위임. `LazyInterceptor`는 `@RuntimeType Object intercept(@AllArguments Object[] args, @Origin Method method)` 시그니처 필요 — Task I2.*

- [ ] **Step 4: PASS 확인 + 커밋**

```bash
git add sfs-orm/src/main/java/com/choisk/sfs/orm/support/LazyProxyFactory.java \
        sfs-orm/src/test/java/com/choisk/sfs/orm/support/LazyProxyFactoryTest.java
git commit -m "feat(sfs-orm): LazyProxyFactory — byte-buddy 서브클래싱 + caching + getId 비-인터셉트 박제"
```

회귀 271 + 3 = 274 PASS.

---

### Task I2: `LazyInterceptor` — target 채우기 + closed 예외 + identity 보장

**Files:**
- Create: `sfs-orm/src/main/java/com/choisk/sfs/orm/support/LazyInterceptor.java`
- Test: `sfs-orm/src/test/java/com/choisk/sfs/orm/support/LazyInterceptorTest.java`

- [ ] **Step 1: 실패 테스트 — 3건**

```java
package com.choisk.sfs.orm.support;

import com.choisk.sfs.orm.annotation.*;
import com.choisk.sfs.orm.annotation.SfsGeneratedValue.GenerationType;
import com.choisk.sfs.orm.exception.SfsLazyInitializationException;
import org.junit.jupiter.api.Test;
import static org.assertj.core.api.Assertions.*;

class LazyInterceptorTest {
    
    @SfsEntity static class Tgt {
        @SfsId @SfsGeneratedValue(strategy = GenerationType.IDENTITY) Long id;
        @SfsColumn String name;
        public String getName() { return name; }
    }
    
    @Test
    void intercept_throws_when_persistenceContext_closed() throws Exception {
        var ctx = new PersistenceContext(); ctx.close();
        var interceptor = new LazyInterceptor(Tgt.class, 1L, ctx);
        
        assertThatThrownBy(() -> interceptor.intercept(
                Tgt.class.getMethod("getName"), new Object[0]))
            .isInstanceOf(SfsLazyInitializationException.class);
    }
    
    @Test
    void intercept_initializes_target_from_context_on_first_call() throws Exception {
        var ctx = new PersistenceContext();
        Tgt original = new Tgt(); original.id = 5L; original.name = "alice";
        ctx.putEntity(new EntityKey(Tgt.class, 5L), original);
        
        var interceptor = new LazyInterceptor(Tgt.class, 5L, ctx);
        Object result = interceptor.intercept(
            Tgt.class.getMethod("getName"), new Object[0]);
        
        assertThat(result).isEqualTo("alice");
    }
    
    @Test
    void intercept_preserves_identity_via_persistenceContext() throws Exception {
        var ctx = new PersistenceContext();
        Tgt original = new Tgt(); original.id = 7L; original.name = "bob";
        ctx.putEntity(new EntityKey(Tgt.class, 7L), original);
        
        var interceptor1 = new LazyInterceptor(Tgt.class, 7L, ctx);
        var interceptor2 = new LazyInterceptor(Tgt.class, 7L, ctx);
        
        // 두 인터셉터 모두 같은 캐시된 target을 받음
        interceptor1.intercept(Tgt.class.getMethod("getName"), new Object[0]);
        interceptor2.intercept(Tgt.class.getMethod("getName"), new Object[0]);
        
        assertThat(interceptor1.target()).isSameAs(interceptor2.target());
    }
}
```

- [ ] **Step 2: FAIL 확인**

- [ ] **Step 3: `LazyInterceptor` 구현**

```java
package com.choisk.sfs.orm.support;

import com.choisk.sfs.orm.exception.SfsLazyInitializationException;
import com.choisk.sfs.orm.exception.SfsPersistenceException;
import net.bytebuddy.implementation.bind.annotation.AllArguments;
import net.bytebuddy.implementation.bind.annotation.Origin;
import net.bytebuddy.implementation.bind.annotation.RuntimeType;

import java.lang.reflect.Method;

public class LazyInterceptor {
    
    private final Class<?> targetClass;
    private final Object pk;
    private final PersistenceContext context;
    private Object target;
    
    public LazyInterceptor(Class<?> targetClass, Object pk, PersistenceContext context) {
        this.targetClass = targetClass;
        this.pk = pk;
        this.context = context;
    }
    
    public Object target() { return target; }
    
    @RuntimeType
    public Object intercept(@Origin Method method, @AllArguments Object[] args) throws Throwable {
        if (target == null) {
            if (context.isClosed()) {
                throw new SfsLazyInitializationException(targetClass.getSimpleName() + "#" + pk);
            }
            // 학습 정점 ③: identityMap 거침 — 같은 PK는 같은 인스턴스
            target = context.getEntity(new EntityKey(targetClass, pk));
            if (target == null) {
                throw new SfsPersistenceException("Lazy target not in persistence context: " + targetClass + "#" + pk);
            }
        }
        try {
            return method.invoke(target, args);
        } catch (java.lang.reflect.InvocationTargetException e) {
            throw e.getTargetException();
        }
    }
}
```

> *주의: 본 단순 구현은 `context.getEntity(key)` 만 사용 — *DB 로드 fallback 없음*. 즉 lazy proxy가 발급된 시점에 *원본이 이미 캐시에 있어야 함*. EntityPersister.loadById()에서 lazy proxy를 생성할 때, 같은 시점에 *원본을 로드하지 않고* proxy만 만들면 후에 LazyInterceptor가 캐시에서 못 찾음. → J1에서 통합 시 *DB 로드 콜백*도 같이 처리.*

- [ ] **Step 4: 통합 시 보강 박제 — DB 로드 fallback 추가**

`LazyInterceptor.intercept()` 내부 보강:

```java
if (target == null) {
    if (context.isClosed()) throw new SfsLazyInitializationException(...);
    target = context.getEntity(new EntityKey(targetClass, pk));
    if (target == null) {
        // DB 로드 — emf의 persister 호출이 필요. 인터셉터에 emf 주입 필요
        // 이 시점에서는 emf가 없으므로 LazyProxyFactory가 emf 알고 있어야 함
        throw new SfsPersistenceException("Lazy target not in persistence context");
    }
}
```

> *현재 단위 테스트 3건은 *원본을 미리 putEntity*하므로 DB 로드 fallback 없이 PASS. J1에서 통합 시 LazyProxyFactory가 emf 참조를 받아 fallback 추가.*

- [ ] **Step 5: PASS 확인 + 커밋**

```bash
git add sfs-orm/src/main/java/com/choisk/sfs/orm/support/LazyInterceptor.java \
        sfs-orm/src/test/java/com/choisk/sfs/orm/support/LazyInterceptorTest.java
git commit -m "feat(sfs-orm): LazyInterceptor — target 채우기 + closed 예외 + identity 보장 (학습 정점 ③ 박제)"
```

회귀 274 + 3 = 277 PASS.

---

### Task J1: `find()` LAZY 채우기 통합 — LazyProxyFactory에 emf 주입 + EAGER 별도 SELECT

**Files:**
- Modify: `sfs-orm/src/main/java/com/choisk/sfs/orm/support/LazyProxyFactory.java`
- Modify: `sfs-orm/src/main/java/com/choisk/sfs/orm/support/LazyInterceptor.java`
- Modify: `sfs-orm/src/main/java/com/choisk/sfs/orm/support/EntityPersister.java`
- Modify: `sfs-orm/src/main/java/com/choisk/sfs/orm/SfsEntityManagerFactory.java`

> *통합 작업. 단위 테스트는 변경 없음 (M3 통합에서 검증). 본 task는 *구현 통합*만, 회귀 +0.*

- [ ] **Step 1: `LazyProxyFactory.createProxy`에 fallback 콜백 받기 — interface로 주입**

`LazyProxyFactory` 생성자 수정:
```java
public interface LazyTargetLoader {
    Object load(Class<?> targetClass, Object pk);
}

public class LazyProxyFactory {
    private final LazyTargetLoader loader;
    public LazyProxyFactory(LazyTargetLoader loader) { this.loader = loader; }
    // ... createProxy()는 LazyInterceptor에 loader 주입
}
```

`LazyInterceptor` 생성자에 loader 추가:
```java
public LazyInterceptor(Class<?> targetClass, Object pk, PersistenceContext context, LazyTargetLoader loader) {
    ...
    this.loader = loader;
}

public Object intercept(...) {
    if (target == null) {
        if (context.isClosed()) throw new SfsLazyInitializationException(...);
        target = context.getEntity(new EntityKey(targetClass, pk));
        if (target == null) {
            target = loader.load(targetClass, pk);   // DB 로드 fallback
            if (target == null) throw new SfsPersistenceException("Target not found: " + targetClass + "#" + pk);
            context.putEntity(new EntityKey(targetClass, pk), target);
        }
    }
    return method.invoke(target, args);
}
```

`SfsEntityManagerFactory` 생성자에서 loader 람다 전달:
```java
this.lazyProxyFactory = new LazyProxyFactory((targetClass, pk) -> {
    EntityPersister p = persisterByClass.get(targetClass);
    return p == null ? null : p.loadById(pk, /* context는 인터셉트 시점 — 여기서는 null */);
});
```

> *컨텍스트는 인터셉트 시점에 결정 — loader 람다에 전달할 방법 필요. 가장 단순: LazyInterceptor가 context를 갖고 있으므로 loader는 그냥 `(cls, pk) -> persisterByClass.get(cls).loadById(pk, null)` — null context는 EntityPersister에서 lazy 채우기를 *하지 않는다*는 신호. 즉 *fallback 로드는 lazy 안 채움* (cycle 회피).*

- [ ] **Step 2: `EntityPersister.buildRowMapper`에 LAZY/EAGER 채우기 추가**

```java
private RowMapper<Object> buildRowMapper(PersistenceContext context, SfsEntityManagerFactory emf) {
    return (rs, rowNum) -> {
        try {
            Object instance = md.entityClass().getDeclaredConstructor().newInstance();
            int idx = 1;
            md.idField().field().set(instance, rs.getObject(idx++, md.idField().javaType()));
            for (FieldMetadata col : md.columns()) {
                col.field().set(instance, rs.getObject(idx++, col.javaType()));
            }
            for (RelationMetadata rel : md.manyToOnes()) {
                Object fk = rs.getObject(idx++);
                if (fk == null) continue;
                if (rel.fetch() == com.choisk.sfs.orm.annotation.SfsManyToOne.FetchType.LAZY) {
                    Object proxy = emf.lazyProxyFactory().createProxy(rel.targetEntity(), fk, context);
                    rel.field().set(instance, proxy);
                } else {
                    // EAGER: 별도 SELECT (재귀 — 1차 캐시 거침)
                    EntityPersister relPersister = emf.persisterOf(rel.targetEntity());
                    Object related = relPersister.loadById(fk, context);
                    if (related != null) {
                        context.putEntity(new EntityKey(rel.targetEntity(), fk), related);
                    }
                    rel.field().set(instance, related);
                }
            }
            return instance;
        } catch (Exception e) {
            throw new SfsPersistenceException("Row mapping 실패: " + md.entityClass().getName(), e);
        }
    };
}
```

`EntityPersister`가 emf를 알아야 하므로 생성자에 추가 + factory에서 주입.

- [ ] **Step 3: 컴파일 + 회귀 (단위 테스트 영향 점검)**

```
./gradlew :sfs-orm:test
```
Expected: 277 PASS (기존 단위 테스트는 lazy 미사용 — 영향 없음)

> *주의: `LazyInterceptorTest`의 3번째 테스트는 *원본을 미리 putEntity*하므로 loader fallback 호출되지 않음. 단 LazyInterceptor 생성자 시그니처 변경되었으니 테스트도 수정 필요. → 테스트 수정: `new LazyInterceptor(Tgt.class, 5L, ctx, (c, p) -> null)` 로 dummy loader 전달.*

- [ ] **Step 4: 커밋**

```bash
git add sfs-orm/src/main/java/com/choisk/sfs/orm/support/{LazyProxyFactory,LazyInterceptor,EntityPersister}.java \
        sfs-orm/src/main/java/com/choisk/sfs/orm/SfsEntityManagerFactory.java \
        sfs-orm/src/test/java/com/choisk/sfs/orm/support/LazyInterceptorTest.java
git commit -m "feat(sfs-orm): find() LAZY/EAGER 통합 — LazyProxyFactory에 loader fallback + EntityPersister row 매핑 (학습 정점 ③ 완성)"
```

회귀 277 PASS.

---

## 섹션 K: remove + flush + merge

### Task K1: `RealEntityManager.remove()`

**Files:** Modify `RealEntityManager.java` only.

> 단위 테스트는 통합 테스트(M1)에서 회귀. 본 task는 구현만.

- [ ] **Step 1: `remove()` 구현**

```java
@Override
public void remove(Object entity) {
    EntityMetadata md = emf.metadataOf(entity.getClass());
    if (md == null) throw new SfsPersistenceException("Unknown entity class");
    
    try {
        Object pk = md.idField().field().get(entity);
        EntityKey key = new EntityKey(entity.getClass(), pk);
        if (!context.contains(key)) {
            throw new IllegalArgumentException("Entity not managed: " + entity);
        }
        context.enqueueAction(new DeleteAction(entity, md));
    } catch (IllegalAccessException e) {
        throw new SfsPersistenceException("Cannot read @SfsId for remove", e);
    }
}
```

- [ ] **Step 2: 컴파일 + 회귀**

```
./gradlew :sfs-orm:compileJava
```

- [ ] **Step 3: 커밋**

```bash
git add sfs-orm/src/main/java/com/choisk/sfs/orm/support/RealEntityManager.java
git commit -m "feat(sfs-orm): RealEntityManager.remove() — actionQueue에 DeleteAction 등록 (flush 시 DELETE)"
```

---

### Task K2: `RealEntityManager.flush()` — Phase 1 dirty + Phase 2 action 실행

**Files:** Modify `RealEntityManager.java`.

> 단위 테스트는 통합 테스트(M1/M2)가 회귀.

- [ ] **Step 1: `flush()` 구현**

```java
@Override
public void flush() {
    // Phase 1: dirty check
    for (var entry : context.identityMap().entrySet()) {
        EntityKey key = entry.getKey();
        Object entity = entry.getValue();
        EntityMetadata md = emf.metadataOf(key.entityClass());
        Object[] current = captureSnapshot(entity, md);
        Object[] original = context.getSnapshot(key);
        BitSet dirty = computeDirty(current, original);
        if (!dirty.isEmpty()) {
            context.enqueueAction(new UpdateAction(entity, md, dirty));
            context.putSnapshot(key, current);
        }
    }
    
    // Phase 2: action 실행 (INSERT → UPDATE → DELETE 순)
    var actions = new ArrayList<>(context.actionQueue());
    actions.sort((a, b) -> typeOrder(a) - typeOrder(b));
    for (EntityAction action : actions) {
        EntityPersister p = emf.persisterOf(action.entity().getClass());
        switch (action) {
            case InsertAction ia -> p.executeInsert(ia.entity());
            case UpdateAction ua -> p.executeUpdate(ua.entity(), ua.dirtyColumns());
            case DeleteAction da -> p.executeDelete(da.entity());
        }
    }
    context.clearActionQueue();
}

private BitSet computeDirty(Object[] current, Object[] original) {
    BitSet dirty = new BitSet();
    for (int i = 0; i < current.length; i++) {
        if (!Objects.equals(current[i], original[i])) dirty.set(i);
    }
    return dirty;
}

private int typeOrder(EntityAction a) {
    return switch (a) {
        case InsertAction ia -> 1;
        case UpdateAction ua -> 2;
        case DeleteAction da -> 3;
    };
}
```

- [ ] **Step 2: 커밋**

```bash
git add sfs-orm/src/main/java/com/choisk/sfs/orm/support/RealEntityManager.java
git commit -m "feat(sfs-orm): RealEntityManager.flush() — dirty check (snapshot 비교) + action 순차 실행 (INSERT→UPDATE→DELETE)"
```

---

### Task K3: `RealEntityManager.merge()` — shallow copy + identity 갱신

**Files:** Modify `RealEntityManager.java`.

- [ ] **Step 1: `merge()` 구현**

```java
@Override
public <T> T merge(T entity) {
    EntityMetadata md = emf.metadataOf(entity.getClass());
    if (md == null) throw new SfsPersistenceException("Unknown entity class");
    
    try {
        Object pk = md.idField().field().get(entity);
        EntityKey key = new EntityKey(entity.getClass(), pk);
        
        Object managed = context.getEntity(key);
        if (managed == null) {
            managed = find(entity.getClass(), pk);
            if (managed == null) {
                throw new SfsPersistenceException("Cannot merge: entity not in DB " + key);
            }
        }
        
        // Shallow copy (cascade 없음, EntityListener 없음 — spec 정합)
        for (FieldMetadata col : md.columns()) {
            col.field().set(managed, col.field().get(entity));
        }
        for (RelationMetadata rel : md.manyToOnes()) {
            rel.field().set(managed, rel.field().get(entity));
        }
        
        // snapshot 갱신
        context.putSnapshot(key, captureSnapshot(managed, md));
        
        @SuppressWarnings("unchecked")
        T result = (T) managed;
        return result;
    } catch (IllegalAccessException e) {
        throw new SfsPersistenceException("Merge failed", e);
    }
}
```

- [ ] **Step 2: 커밋**

```bash
git add sfs-orm/src/main/java/com/choisk/sfs/orm/support/RealEntityManager.java
git commit -m "feat(sfs-orm): RealEntityManager.merge() — shallow copy + identity 갱신 (return value 함정 박제)"
```

회귀 277 PASS (구현만 — 통합 테스트가 검증).

---

## 섹션 L: boot — TSM 통합

### Task L1: `SfsEntityManagerFactoryBean` — bean lifecycle + TSM 콜백

**Files:**
- Create: `sfs-orm/src/main/java/com/choisk/sfs/orm/boot/SfsEntityManagerFactoryBean.java`

> 단위 테스트는 통합 테스트(M1+)가 검증.

- [ ] **Step 1: 구현**

```java
package com.choisk.sfs.orm.boot;

import com.choisk.sfs.orm.SfsEntityManager;
import com.choisk.sfs.orm.SfsEntityManagerFactory;
import com.choisk.sfs.orm.support.RealEntityManager;
import com.choisk.sfs.tx.support.TransactionSynchronizationManager;
import com.choisk.sfs.tx.support.TransactionSynchronization;

public class SfsEntityManagerFactoryBean {
    
    private final SfsEntityManagerFactory emf;
    private final TransactionSynchronizationManager tsm;
    
    public SfsEntityManagerFactoryBean(SfsEntityManagerFactory emf, TransactionSynchronizationManager tsm) {
        this.emf = emf;
        this.tsm = tsm;
    }
    
    public SfsEntityManagerFactory factory() { return emf; }
    
    /**
     * 트랜잭션 시작 시 EM 생성 + TSM resource로 등록 + 트랜잭션 종료 시 flush + close 콜백 등록.
     * SfsTransactionalEntityManager가 호출.
     */
    public RealEntityManager bindToCurrentTransaction() {
        Object existing = tsm.getResource(emf);
        if (existing != null) return (RealEntityManager) existing;
        
        RealEntityManager em = (RealEntityManager) emf.createEntityManager();
        tsm.bindResource(emf, em);
        
        // 트랜잭션 종료 콜백 (flush + close)
        tsm.registerSynchronization(new TransactionSynchronization() {
            @Override public void beforeCommit() { em.flush(); }
            @Override public void afterCompletion(int status) {
                em.context().close();
                tsm.unbindResource(emf);
            }
        });
        
        return em;
    }
}
```

> *`TransactionSynchronization` 인터페이스가 sfs-tx에 정의되어 있어야 함. Phase 3에서 도입했을 가능성 — 없으면 sfs-tx에 추가 필요.*

- [ ] **Step 2: 컴파일 + 커밋**

```bash
git add sfs-orm/src/main/java/com/choisk/sfs/orm/boot/SfsEntityManagerFactoryBean.java
git commit -m "feat(sfs-orm): SfsEntityManagerFactoryBean — TSM resource 등록 + beforeCommit flush + afterCompletion close"
```

---

### Task L2: `SfsTransactionalEntityManager` — proxy + TSM lookup

**Files:**
- Create: `sfs-orm/src/main/java/com/choisk/sfs/orm/boot/SfsTransactionalEntityManager.java`

- [ ] **Step 1: 구현**

```java
package com.choisk.sfs.orm.boot;

import com.choisk.sfs.orm.SfsEntityManager;
import com.choisk.sfs.orm.exception.SfsTransactionRequiredException;
import com.choisk.sfs.tx.support.TransactionSynchronizationManager;

public class SfsTransactionalEntityManager implements SfsEntityManager {
    
    private final SfsEntityManagerFactoryBean factoryBean;
    private final TransactionSynchronizationManager tsm;
    
    public SfsTransactionalEntityManager(SfsEntityManagerFactoryBean factoryBean,
                                         TransactionSynchronizationManager tsm) {
        this.factoryBean = factoryBean;
        this.tsm = tsm;
    }
    
    private SfsEntityManager currentEm() {
        if (!tsm.isActualTransactionActive()) {
            throw new SfsTransactionRequiredException();
        }
        return factoryBean.bindToCurrentTransaction();
    }
    
    @Override public void persist(Object entity) { currentEm().persist(entity); }
    @Override public <T> T find(Class<T> ec, Object pk) { return currentEm().find(ec, pk); }
    @Override public void remove(Object entity) { currentEm().remove(entity); }
    @Override public void flush() { currentEm().flush(); }
    @Override public <T> T merge(T entity) { return currentEm().merge(entity); }
    @Override public boolean contains(Object entity) { return currentEm().contains(entity); }
}
```

> *`tsm.isActualTransactionActive()`가 sfs-tx에 있어야 함. 없으면 `tsm.getResource(...)` null 체크로 대체.*

- [ ] **Step 2: 커밋**

```bash
git add sfs-orm/src/main/java/com/choisk/sfs/orm/boot/SfsTransactionalEntityManager.java
git commit -m "feat(sfs-orm): SfsTransactionalEntityManager — TSM lookup + TransactionRequiredException (L1 통합 완성)"
```

---

## 섹션 M: 통합 테스트 (5 파일, 19건)

### Task M1: `BasicCrudIntegrationTest` (5건)

**Files:**
- Create: `sfs-orm/src/test/resources/orm-schema-test.sql`
- Create: `sfs-orm/src/test/java/com/choisk/sfs/orm/integration/BasicCrudIntegrationTest.java`

- [ ] **Step 1: 공통 schema 작성**

`sfs-orm/src/test/resources/orm-schema-test.sql`:
```sql
DROP TABLE IF EXISTS test_orders;
DROP TABLE IF EXISTS test_users;
DROP SEQUENCE IF EXISTS test_users_seq;

CREATE SEQUENCE test_users_seq START WITH 1 INCREMENT BY 1;

CREATE TABLE test_users (
    id BIGINT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    email VARCHAR(200)
);

CREATE TABLE test_orders (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    user_id BIGINT,
    amount DECIMAL(10,2) NOT NULL,
    status VARCHAR(20) NOT NULL,
    FOREIGN KEY (user_id) REFERENCES test_users(id)
);
```

- [ ] **Step 2: BasicCrudIntegrationTest 5건 작성**

```java
package com.choisk.sfs.orm.integration;

import com.choisk.sfs.orm.SfsEntityManager;
import com.choisk.sfs.orm.SfsEntityManagerFactory;
import com.choisk.sfs.orm.annotation.*;
import com.choisk.sfs.orm.annotation.SfsGeneratedValue.GenerationType;
import com.choisk.sfs.orm.annotation.SfsManyToOne.FetchType;
import com.choisk.sfs.orm.boot.*;
import com.choisk.sfs.tx.jdbc.JdbcTemplate;
import com.choisk.sfs.tx.support.*;
import org.h2.jdbcx.JdbcDataSource;
import org.h2.tools.RunScript;
import org.junit.jupiter.api.*;
import static org.assertj.core.api.Assertions.*;

import java.io.InputStreamReader;
import java.math.BigDecimal;

@TestInstance(TestInstance.Lifecycle.PER_CLASS)
class BasicCrudIntegrationTest {
    
    @SfsEntity(name = "test_users")
    public static class TestUser {
        @SfsId @SfsGeneratedValue(strategy = GenerationType.SEQUENCE, sequenceName = "test_users_seq") Long id;
        @SfsColumn String name;
        @SfsColumn String email;
        public Long getId() { return id; }
    }
    
    @SfsEntity(name = "test_orders")
    public static class TestOrder {
        @SfsId @SfsGeneratedValue(strategy = GenerationType.IDENTITY) Long id;
        @SfsManyToOne(fetch = FetchType.LAZY) @SfsJoinColumn(name = "user_id") TestUser user;
        @SfsColumn BigDecimal amount;
        @SfsColumn String status;
        public Long getId() { return id; }
    }
    
    private JdbcDataSource ds;
    private TransactionSynchronizationManager tsm;
    private DataSourceTransactionManager tm;
    private SfsEntityManagerFactory emf;
    private SfsEntityManagerFactoryBean factoryBean;
    private SfsTransactionalEntityManager em;
    private JdbcTemplate jdbc;
    
    @BeforeAll
    void setupAll() throws Exception {
        ds = new JdbcDataSource();
        ds.setURL("jdbc:h2:mem:basiccrud;DB_CLOSE_DELAY=-1");
        try (var conn = ds.getConnection();
             var reader = new InputStreamReader(getClass().getResourceAsStream("/orm-schema-test.sql"))) {
            RunScript.execute(conn, reader);
        }
        tsm = new ThreadLocalTsm();
        tm = new DataSourceTransactionManager(ds, tsm);
        jdbc = new JdbcTemplate(ds, tsm);
        
        emf = SfsEntityManagerFactory.builder()
            .dataSource(ds).transactionSynchronizationManager(tsm)
            .addEntityClass(TestUser.class).addEntityClass(TestOrder.class)
            .build();
        factoryBean = new SfsEntityManagerFactoryBean(emf, tsm);
        em = new SfsTransactionalEntityManager(factoryBean, tsm);
    }
    
    @BeforeEach
    void cleanup() {
        jdbc.update("DELETE FROM test_orders");
        jdbc.update("DELETE FROM test_users");
    }
    
    @Test
    void persist_and_find_returns_same_instance_in_same_transaction() {
        TransactionTemplate.execute(tm, () -> {
            TestUser u = new TestUser(); u.name = "alice"; u.email = "a@x.com";
            em.persist(u);
            assertThat(em.find(TestUser.class, u.getId())).isSameAs(u);
            return null;
        });
    }
    
    @Test
    void persist_then_flush_inserts_to_db() {
        TransactionTemplate.execute(tm, () -> {
            TestUser u = new TestUser(); u.name = "bob"; u.email = "b@x.com";
            em.persist(u);
            em.flush();
            Long count = jdbc.queryForObject("SELECT COUNT(*) FROM test_users WHERE name = 'bob'", Long.class);
            assertThat(count).isEqualTo(1L);
            return null;
        });
    }
    
    @Test
    void find_returns_null_when_not_in_db() {
        TransactionTemplate.execute(tm, () -> {
            assertThat(em.find(TestUser.class, 9999L)).isNull();
            return null;
        });
    }
    
    @Test
    void remove_then_flush_deletes_from_db() {
        TransactionTemplate.execute(tm, () -> {
            TestUser u = new TestUser(); u.name = "to_delete";
            em.persist(u);
            em.flush();
            em.remove(u);
            em.flush();
            Long count = jdbc.queryForObject("SELECT COUNT(*) FROM test_users WHERE id = ?", Long.class, u.getId());
            assertThat(count).isEqualTo(0L);
            return null;
        });
    }
    
    @Test
    void persist_without_transaction_throws_TransactionRequiredException() {
        assertThatThrownBy(() -> em.persist(new TestUser()))
            .isInstanceOf(com.choisk.sfs.orm.exception.SfsTransactionRequiredException.class);
    }
}
```

- [ ] **Step 3: PASS 확인 + 커밋**

```
./gradlew :sfs-orm:test --tests BasicCrudIntegrationTest
```
Expected: 5건 PASS

```bash
git add sfs-orm/src/test/resources/orm-schema-test.sql \
        sfs-orm/src/test/java/com/choisk/sfs/orm/integration/BasicCrudIntegrationTest.java
git commit -m "test(sfs-orm): BasicCrudIntegrationTest 5건 — persist/find/remove/flush/TxRequired (M1)"
```

회귀 277 + 5 = 282 PASS.

---

### Task M2: `DirtyCheckingIntegrationTest` (3건) + `IdentityMapIntegrationTest` (3건)

**Files:**
- Create: `sfs-orm/src/test/java/com/choisk/sfs/orm/integration/DirtyCheckingIntegrationTest.java`
- Create: `sfs-orm/src/test/java/com/choisk/sfs/orm/integration/IdentityMapIntegrationTest.java`

- [ ] **Step 1: DirtyCheckingIntegrationTest 작성 — setup 헬퍼 BasicCrud에서 추출 권장**

```java
@Test
void single_dirty_field_generates_UPDATE_on_flush() {
    TestUser persisted = TransactionTemplate.execute(tm, () -> {
        TestUser u = new TestUser(); u.name = "x"; u.email = "x@x.com";
        em.persist(u); em.flush();
        return u;
    });
    
    TransactionTemplate.execute(tm, () -> {
        TestUser u = em.find(TestUser.class, persisted.id);
        u.name = "updated_name";
        em.flush();
        String name = jdbc.queryForObject("SELECT name FROM test_users WHERE id = ?", String.class, persisted.id);
        assertThat(name).isEqualTo("updated_name");
        return null;
    });
}

@Test
void multiple_dirty_fields_generate_UPDATE() { /* name + email 둘 다 변경 */ }

@Test
void no_dirty_fields_skips_UPDATE() {
    /* JdbcTemplate spy 또는 @AfterEach에서 SQL 카운트 검증 */
    /* 단순화: flush 호출해도 변경사항 없으면 DB 변경 없음 */
}
```

- [ ] **Step 2: IdentityMapIntegrationTest — 3건 (find 두 번 == / proxy ↔ 원본 == / EM 다르면 ≠)**

```java
@Test
void find_twice_returns_same_instance() {
    TestUser persisted = TransactionTemplate.execute(tm, () -> {
        TestUser u = new TestUser(); u.name = "alice";
        em.persist(u); em.flush();
        return u;
    });
    
    TransactionTemplate.execute(tm, () -> {
        TestUser u1 = em.find(TestUser.class, persisted.id);
        TestUser u2 = em.find(TestUser.class, persisted.id);
        assertThat(u1).isSameAs(u2);
        return null;
    });
}

@Test
void lazy_proxy_target_equals_directly_loaded_entity() {
    /* Order의 lazy user가 같은 트랜잭션의 find(User) 와 == */
    Long userId, orderId;
    TransactionTemplate.execute(tm, () -> {
        TestUser u = new TestUser(); u.name = "bob";
        em.persist(u); em.flush();
        TestOrder o = new TestOrder(); o.user = u; o.amount = BigDecimal.ONE; o.status = "X";
        em.persist(o); em.flush();
        return new long[]{u.id, o.id};
    });
    /* ... */
}

@Test
void different_persistence_contexts_yield_different_instances() {
    /* 두 트랜잭션에서 같은 PK find → 다른 인스턴스 */
}
```

- [ ] **Step 3: PASS + 커밋**

```bash
git add sfs-orm/src/test/java/com/choisk/sfs/orm/integration/{DirtyCheckingIntegrationTest,IdentityMapIntegrationTest}.java
git commit -m "test(sfs-orm): DirtyChecking + IdentityMap 통합 테스트 6건 — 학습 정점 ② 박제 (M2)"
```

회귀 282 + 6 = 288 PASS.

---

### Task M3: `LazyLoadingIntegrationTest` (4건) + `LazyInitializationExceptionTest` (2건)

**Files:**
- Create: `sfs-orm/src/test/java/com/choisk/sfs/orm/integration/LazyLoadingIntegrationTest.java`
- Create: `sfs-orm/src/test/java/com/choisk/sfs/orm/integration/LazyInitializationExceptionTest.java`

- [ ] **Step 1: LazyLoadingIntegrationTest 4건**
  - LAZY 첫 호출 시 SELECT 발생 (별도 EAGER 검증 entity 추가 필요)
  - EAGER 별도 SELECT 발생
  - LAZY proxy의 getId()는 SELECT 없음
  - LAZY + EAGER 혼합

- [ ] **Step 2: LazyInitializationExceptionTest 2건**
  - 트랜잭션 종료 후 lazy 접근 → SfsLazyInitializationException
  - 트랜잭션 안에서도 EM close 명시 호출 후 → 예외

(상세 코드는 spec § 4.7 + § 5.4 시나리오 정합)

- [ ] **Step 3: PASS + 커밋**

```bash
git commit -m "test(sfs-orm): LazyLoading + LazyInitException 통합 테스트 6건 — 학습 정점 ③ 박제 (M3)"
```

회귀 288 + 6 = 294 PASS.

---

### Task M4: `MergeIntegrationTest` (3건) + `TransactionRequiredExceptionTest` (1건)

**Files:**
- Create: `sfs-orm/src/test/java/com/choisk/sfs/orm/integration/MergeIntegrationTest.java`
- Create: `sfs-orm/src/test/java/com/choisk/sfs/orm/integration/TransactionRequiredExceptionTest.java`

- [ ] **Step 1: MergeIntegrationTest 3건**
  - merge 정상 — return value 받아서 변경 반영
  - return 무시 함정 — detached 변경은 사라짐
  - snapshot 갱신 검증 — merge 후 dirty check 동작

- [ ] **Step 2: TransactionRequiredExceptionTest 1건**
  - `em.find()` 트랜잭션 없이 호출 → SfsTransactionRequiredException

- [ ] **Step 3: PASS + 커밋**

```bash
git commit -m "test(sfs-orm): Merge + TxRequired 통합 테스트 4건 — merge 함정 박제 (M4)"
```

회귀 294 + 4 = 298 PASS.

> *목표 +52 초과 — 296에서 298이 됨. 일부 테스트가 다른 task에서 흡수되거나 spec § 7.2의 +50과 차이 발생 시 plan/spec 정정 박제.*

---

### Task M5: `SequenceVsIdentityComparisonTest` (2건)

**Files:**
- Create: `sfs-orm/src/test/java/com/choisk/sfs/orm/integration/SequenceVsIdentityComparisonTest.java`

- [ ] **Step 1: 비교 박제 테스트 2건**

```java
@Test
void SEQUENCE_persist_assigns_id_but_does_not_insert_until_flush() {
    /* 학습 정점 ① 정상 박제 — User entity */
}

@Test
void IDENTITY_persist_inserts_immediately_breaking_write_behind() {
    /* 학습 정점 ① 깨짐 박제 — Order entity */
}
```

- [ ] **Step 2: 커밋**

```bash
git commit -m "test(sfs-orm): SequenceVsIdentityComparisonTest 2건 — 학습 정점 ① 정상 vs 깨짐 비교 박제 (M5)"
```

회귀 298 + 2 = 300 PASS.

> *최종 +56 PASS — 목표 +52 초과 4건. spec § 7.4 정정 또는 일부 통합 테스트 시나리오 압축 검토.*

---

## 섹션 N: sfs-samples demo

### Task N1: 도메인 + schema + OrmConfig

**Files:**
- Create: `sfs-samples/src/main/java/com/choisk/sfs/samples/orm/domain/{User,Order,AuditLog}.java`
- Create: `sfs-samples/src/main/java/com/choisk/sfs/samples/orm/config/OrmConfig.java`
- Create: `sfs-samples/src/main/resources/orm-schema.sql`
- Modify: `sfs-samples/build.gradle.kts` (sfs-orm 의존 추가)

> TDD 제외 (단순 도메인 + 설정).

- [ ] **Step 1: build.gradle.kts에 sfs-orm 추가**

```kotlin
dependencies {
    implementation(project(":sfs-context"))
    implementation(project(":sfs-aop"))
    implementation(project(":sfs-tx"))
    implementation(project(":sfs-orm"))    // ← 추가
    implementation(libs.h2)
    testImplementation(libs.h2)
}
```

- [ ] **Step 2: orm-schema.sql 작성** (spec § 5.2 정합)

- [ ] **Step 3: User/Order/AuditLog 도메인 작성** (spec § 5.3 정합)

- [ ] **Step 4: OrmConfig 작성** (spec § 2.7 정합)

- [ ] **Step 5: 컴파일 + 커밋**

```bash
git add sfs-samples/build.gradle.kts \
        sfs-samples/src/main/java/com/choisk/sfs/samples/orm/ \
        sfs-samples/src/main/resources/orm-schema.sql
git commit -m "feat(sfs-samples): ORM demo 도메인 (User/Order/AuditLog) + schema + OrmConfig"
```

---

### Task N2: `OrmDemoApplication` main + service 7 시나리오

**Files:**
- Create: `sfs-samples/src/main/java/com/choisk/sfs/samples/orm/OrmDemoApplication.java`
- Create: `sfs-samples/src/main/java/com/choisk/sfs/samples/orm/service/{OrderService,UserService}.java`

- [ ] **Step 1: service 메서드 7개 작성** (spec § 5.4 시연 7건 정합)
- [ ] **Step 2: main 메서드 — try-catch + console output**
- [ ] **Step 3: manual run 검증**

```bash
./gradlew :sfs-samples:run -PmainClass=com.choisk.sfs.samples.orm.OrmDemoApplication
```

console output이 spec § 5.5 패턴 대로 7건 시연.

- [ ] **Step 4: 커밋**

```bash
git commit -m "feat(sfs-samples): OrmDemoApplication — 학습 정점 ①②③ console 시연 7건"
```

---

## 섹션 O: 마감

### Task O1: 전체 빌드 + 회귀 + DoD 체크박스 갱신

- [ ] **Step 1: 전체 빌드**

```
./gradlew clean build
```
Expected: BUILD SUCCESSFUL, 회귀 ~298~300 PASS / 0 FAIL

- [ ] **Step 2: DoD 14항목 체크박스 갱신** (spec § 8 정합)

본 plan 문서 + spec § 8 모두 갱신.

- [ ] **Step 3: 회귀 카운트가 spec § 7.4 +52와 차이 시 박제**

만약 +56 (4건 초과) 발생 시:
- spec § 7.4 정정 (실제 카운트로) + 박제 한 줄
- 또는 통합 테스트 시나리오 압축 (M2/M3에서 중복 흡수)

- [ ] **Step 4: 커밋**

```bash
git add docs/superpowers/plans/2026-05-04-phase-4-orm.md docs/superpowers/specs/2026-05-04-phase-4-orm-design.md
git commit -m "docs: Phase 4 DoD 체크박스 갱신 + 회귀 카운트 박제 (실제 N PASS / 0 FAIL)"
```

---

### Task O2: 마감 게이트 3단계 (다관점 리뷰 + 리팩토링 + simplify)

CLAUDE.md *"완료 후 품질 게이트"* 정합.

- [ ] **Step 1: 다관점 코드리뷰** — 아래 5관점 각각 독립적으로:
  - 아키텍처·설계 일관성 (sfs-orm 의존 정합, Hibernate 본가 패턴 박제 정확성)
  - 가독성·네이밍·주석 WHY (CLAUDE.md 주석 규칙)
  - 테스트 커버리지 (TDD 적용/제외 판단 정합)
  - 동시성·라이프사이클·실패 복구 (close/destroy 경로)
  - 보안·리소스 누수 (Connection 누수, byte-buddy 클래스 leak)

  실행: `feature-dev:code-reviewer` 에이전트 (자동 스캔) + `superpowers:requesting-code-review` (외부 관점). 결과 *"남겨둘 / 즉시 고칠"* 분류.

- [ ] **Step 2: 리팩토링** — 즉시 고칠 이슈만 반영. 동작 변경 없음. 커밋 `refactor(sfs-orm): ...`

- [ ] **Step 3: `/simplify` 패스** — 재사용 패턴 추출, 중복 제거, 데드 코드 정리. diff 단위 검토 후 가치 있는 것만 반영. 커밋 `refactor(sfs-orm): ...` 또는 `chore: ...`

- [ ] **Step 4: 게이트 통과 기준**:
  - `./gradlew build` 전체 PASS
  - 회귀 카운트가 plan DoD 예상치와 일치
  - 본 plan + spec DoD 14항목 모두 `[x]`
  - 본 plan 하단에 `> **품질 게이트 기록 (YYYY-MM-DD):**` 블록 박제 (지적사항 N건 / 반영 M건 / 보류 K건)

- [ ] **Step 5: 메모리 박제 + main 머지 준비**

```bash
git commit -m "docs: Plan § 11 품질 게이트 기록 박제 + DoD 14 [x] — Phase 4 ORM 마감"
```

`MEMORY.md` + `project_phase4_resume_point.md` 신설:
- Phase 4 마감 — 회귀 244 → ~300 PASS, DoD 14/14, 마감 게이트 통과
- 학습 정점 박제: ①②③ + Phase 1A gap 책임 분담의 *대칭 회수* 성취
- mini-phase 후보 9건 활성

main 머지는 *사용자 직접 진행* (Phase 2B/3/1A gap 동일 패턴).

---

## 11. Plan ↔ Spec ↔ DoD 매핑 점검

| spec § | plan task | DoD |
|---|---|---|
| § 2.1~2.3 (모듈/패키지) | A1 | 1 |
| § 2.4 (API 시그니처) | C1 | 10 |
| § 2.5 (Lifecycle) + § 6.4 (Connection) | L1, L2 | 9 |
| § 3.1 (PersistenceContext) | D1 | 5 |
| § 3.2 (EntityMetadata) | B1, B2 | 4, 5 |
| § 3.3 (EntityPersister) | F1, F2 | 6 |
| § 3.4 (LazyProxyFactory + Interceptor) | I1, I2, J1 | 7 |
| § 3.5 (IdentifierGenerator) | E1, E2 | 8 |
| § 4.1, 4.2 (persist flow) | G1, G2 | 7 |
| § 4.3 (find flow) | H1, J1 | 7 |
| § 4.4 (remove) | K1 | 10 |
| § 4.5 (merge) | K3 | 10 |
| § 4.6 (flush) | K2 | 5 |
| § 5 (demo) | N1, N2 | 11 |
| § 6 (예외) | A3 + 통합 테스트 (M3, M4) | 3 |
| § 6.2 (fail-fast) | B2 | 4 |
| § 6.4 (Connection sfs-tx 위임) | PRE + L1 | 1.5, 9 |
| § 7 (테스트) | M1~M5 | 12 |
| § 8 (DoD) | O1 | 14 (마감) |
| § 9 (이월 박제) | (해당 없음 — 별도 phase) | - |

---

## 12. 회귀 카운트 진행 표

| Task 완료 | 누적 PASS |
|---|---|
| 시작 (Phase 1A gap 마감) | 244 |
| PRE (sfs-tx +2) | 246 |
| A1, A2, A3 (TDD 제외) | 246 |
| B1 (TDD 제외) | 246 |
| B2 (+8) | 254 |
| C1 (TDD 제외 + 통합 흡수) | 254 |
| D1 (+6) | 260 |
| E1 (+2) | 262 |
| E2 (+2) | 264 |
| F1 (+3) | 267 |
| F2 (+2) | 269 |
| G1 (+1) | 270 |
| G2 (+1) | 271 |
| H1 (구현만) | 271 |
| I1 (+3) | 274 |
| I2 (+3) | 277 |
| J1 (구현만) | 277 |
| K1, K2, K3 (구현만) | 277 |
| L1, L2 (구현만) | 277 |
| M1 (+5) | 282 |
| M2 (+6) | 288 |
| M3 (+6) | 294 |
| M4 (+4) | 298 |
| M5 (+2) | 300 |
| N1, N2 (TDD 제외) | 300 |
| O1, O2 (마감) | 300 |

**최종 +56 PASS (244 → 300)** — spec § 7.4 *"+50"*과 차이 6건. 본 plan 작성 중 보강된 시나리오 (PRE +2 / G1+G2 단위 +2 / K2 K3 통합 흡수 +0). spec 정정은 O1에서 박제.
