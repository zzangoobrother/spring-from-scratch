package com.choisk.sfs.orm.support;

import com.choisk.sfs.tx.jdbc.JdbcTemplate;

/**
 * 엔티티 INSERT/DELETE SQL 실행 담당 stub — F1 이후 본격 구현.
 */
public class EntityPersister {

    public EntityPersister(EntityMetadata md, IdentifierGenerator gen, JdbcTemplate jdbc) {
        /* F1+ */
    }
}
